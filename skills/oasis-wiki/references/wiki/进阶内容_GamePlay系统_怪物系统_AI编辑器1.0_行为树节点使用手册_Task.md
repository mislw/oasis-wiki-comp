# 进阶内容/GamePlay系统/怪物系统/AI编辑器1.0/行为树节点使用手册/Task

> 本分类共 2 篇文章

---


## 引擎

> 文档路径: 进阶内容 > GamePlay系统 > 怪物系统 > AI编辑器1.0 > 行为树节点使用手册 > Task > 引擎

#  引擎

## Make Noise

如果受控Pawn拥有 **PawnNoiseEmitter** 组件，**发出噪音（Make Noise）** 任务将使Pawn"产生噪声"（发送消息），使其它拥有 **PawnSensing** 组件的Pawn听到（接收消息）。

---

## Move Directly Toward

**直接移动至（Move Directly Toward）** 任务节点会将AI Pawn沿直线移向指定的Actor或位置（矢量）黑板条目，而不参考任何导航系统。如果需要AI按导航移动，请改用 **移动至（Move To）** 任务节点。

---

## Move To

**移动至（Move To）** 任务将使拥有角色移动组件的Pawn使用寻路网格体移动至矢量黑板键。

---

## Play Animation

**播放动画（Play Animation）** 节点可用于播放指定的动画资源。

---

## Play Sound

**播放音效（Play Sound）** 节点会播放在 **要播放的音效（Sound to Play）** 属性中给定的音效。

---

## Push Pawn Action

**推送Pawn动作（Push Pawn Action）** 节点使您能够将指定的动作推送至Pawn的控制器

---

## Rotate to face BB entry

**旋转至面向黑板条目（Rotate to face BB entry）** 任务节点会使关连Pawn向指定的黑板键旋转

---

## Run Behavior

利用 **运行行为（Run Behavior）** 任务节点可以将分支树推送到执行堆栈上，从而运行另一个行为树。然而，要考虑的一个限制是在运行时不能改变该子树资源。存在该限制的原因是给子树的根等级装饰器节点提供支持，这些装饰器节点将注入到父树中。此外，在运行时不能修改运行树的结构。

---

## Run Behavior Dynamic

**运行动态行为（Run Behavior Dynamic）** 任务节点使您能够在执行堆栈上推送子树。使用 **行为树组件** 上的 **SetDynamicSubtree** 函数即可以在运行时分配子树资源。

---

## Run EQS Query

当任务节点被执行时，**运行EQS查询（Run EQS Query）** 节点将运行指定的 场景查询系统（EQS）资源。

| Run Mode | 描述 |
| :--- | :---: |
| **SingleResult** | 选择得分最高的第一个项目 |
| **RandomBest5Pct** | 从得分在总分95% 至 100% 的项目中随机选择 |
| **RandomBest25Pct** | 从得分在总分75% 至 100% 的项目中随机选择 |
| **AllMatching** | 获取所有符合条件的项目 |

---

## Set Tag Cooldown

设置 **冷却标签（Cooldown Tag）** 数值，并与 **冷却标签装饰器（Cooldown Tag Decorators）** 一起使用，从而防止行为树的执行。

---

## Wait

**等待（Wait）** 任务节点可以在行为树中使用，使树在此节点上等待，直至指定的 **等待时间（Wait Time）** 结束。

---

## Wait Blackboard Time

与 **等待（Wait）** 任务节点的原理类似，但该节点会拉取等待时间黑板值。



---


## 和平

> 文档路径: 进阶内容 > GamePlay系统 > 怪物系统 > AI编辑器1.0 > 行为树节点使用手册 > Task > 和平

#  和平

# 通用

## 打印日志（UGC_OutputLog）

可以当前行为树相关信息到日志中

## 等待（UGC_Wait）

**等待（UGC_Wait）** 任务节点可以在行为树中使用，使树在此节点上等待，直至指定的 **等待时间（Wait Time）** 结束。

## 修改黑板变量值（UGC_ModifyBBValue）

修改指定黑板变量的变量值

---

# 角色

## 对准朝向（UGC_SetFocus）

按照一定的速度转向目标。

## 获取随机点（UGC_GetRandomLocation）

获得指定目标附近的随机点。

## 射击(UGC_Shooting)

射击

## 释放技能（UGC_CastSkill）

释放指定类型的技能。
注意事项：该节点通过技能实现，需要pawn上配置对应的技能

## 寻找指定类型Actor（UGC_GetActorOfClass）

寻找周围指定类型的Actor

---

# 怪物

## 怪物移动(UGC_MonsterMove)

怪物移动到指定目标位置

## 装备武器(UGC_EquipWeapon)

怪物装备武器，不走背包

---

# 主角

## 主角移动(UGC_PlayerMove)

控制主角移动到指定位置

## 刷物品(UGC_SpawnItem)

生成道具

## 近战攻击(UGC_MeleeAttack)

主角释放近战攻击
注意事项：该节点通过技能实现，需要pawn上配置对应的技能

## 设置角色状态(UGC_SetPawnState)

设置主角的状态，比如游泳，战斗，拾取等等

## 设置角色姿势(UGC_SetPose)

设置主角的姿势，比如站立，跳跃，下蹲等

## 吃药(UGC_TakeMedicine)

注意事项：该节点通过技能实现，需要pawn上配置对应的技能

| 属性 | 描述 |
| :--- | :---: |
| **药品类型** | 绷带，止痛药等 |
| **Ignore Restart Self** | 启用后，当选择执行的任务已在运行时，任务搜索将被放弃 |
| **Node Name** | 节点在行为树图表中显示的名称 |

## 换弹(UGC_ReloadWeapon)

| 属性 | 描述 |
| :--- | :---: |
| **Ignore Restart Self** | 启用后，当选择执行的任务已在运行时，任务搜索将被放弃 |
| **Node Name** | 节点在行为树图表中显示的名称 |

## 查找扔投掷物落点(UGC_FindThrowProjectileLocRot)

| 属性 | 描述 |
| :--- | :---: |
| **投掷参考点** | 投掷参考点，必填 |
| **投掷物物品ID** | 投掷物物品ID |
| **投掷模式** | 投掷模式 |
| **随机半径最小值** | 随机半径最小值 |
| **随机半径最大值** | 随机半径最大值 |
| **随机最大迭代次数** | 随机最大迭代次数 |
| **查找地表通道** | 查找地表通道 |
| **目标点地表偏移** | 目标点地表偏移 |
| **有效目标点高度偏移** | 有效目标点高度偏移 |
| **输出目标位置** | 输出目标位置，必填 |
| **输出目标朝向** | 输出目标朝向，必填 |
| **Ignore Restart Self** | 启用后，当选择执行的任务已在运行时，任务搜索将被放弃 |
| **Node Name** | 节点在行为树图表中显示的名称 |

## 扔投掷物(UGC_ThrowProjectile)

| 属性 | 描述 |
| :--- | :---: |
| **投掷物物品ID** | 投掷物物品ID，必填 |
| **投掷模式** | 投掷模式 |
| **投掷目标位置** | 投掷目标位置，必填 |
| **投掷目标朝向** | 投掷目标朝向，必填 |
| **投掷结束是否切回原武器** | 投掷结束是否切回原武器 |
| **切武器时间** | 切武器时间 |
| **投掷前随机等待时间** | 投掷前随机等待时间 |
| **投掷后随机等待时间** | 投掷后随机等待时间 |
| **Ignore Restart Self** | 启用后，当选择执行的任务已在运行时，任务搜索将被放弃 |
| **Node Name** | 节点在行为树图表中显示的名称 |

## 换弹(UGC_SwitchWeapon)

| 属性 | 描述 |
| :--- | :---: |
| **目标武器槽位** | 目标武器槽位 |
| **是否使用动画** | 是否使用动画 |
| **是否强制结束先前的切换** | 是否强制结束先前的切换 |
| **切换超时时间** | 切换超时时间 |
| **状态结束延迟时间** | 状态结束延迟时间 |
| **Ignore Restart Self** | 启用后，当选择执行的任务已在运行时，任务搜索将被放弃 |
| **Node Name** | 节点在行为树图表中显示的名称 |

---



---
