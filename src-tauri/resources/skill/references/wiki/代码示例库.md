# 绿洲启元 Lua 代码示例库

> 从 278 篇文档中提取的代码示例

---


## 匹配设置

> 来源: 绿洲编辑器基础内容/匹配系统 (id:127)


### 示例 1

```lua
---玩家匹配分                            
---生效范围: S                                                      
    PlayerState.UGCPlayerInfo.MatchRank --获得每个玩家的PlayerState之后可用此变量读写玩家的自定义匹配分
```


## 让怪物移动起来

> 来源: 进阶内容/GamePlay系统/怪物系统/AI编辑器1.0/怪物制作 (id:171)


### 示例 1

```lua
local MyAIC = {}; 

function MyAIC:GetBehaviorTreeObjectPath()
    return string.format('%sAsset/Blueprint/MyBT.MyBT', UGCMapInfoLib.GetRootLongPackagePath())
end

function MyAIC:OnPossess()
    self.SuperClass.OnPossess(self)

    self:RunBehaviorTree(UE.LoadObject(self:GetBehaviorTreeObjectPath()))
end

return MyAIC;
```


## 快速入门

> 来源: 进阶内容/GamePlay系统/技能系统/技能编辑器1.0 (id:178)


### 示例 1

```lua
--获取技能管理器
local SkillManager = self:GetSkillManagerComponent()
if SkillManager ~= nil then
	--释放120号技能
	SkillManager:TriggerEvent(120, UTSkillEventType.SET_KEY_DOWN)
end
```


## 利用和平的角色拓展玩法内容

> 来源: 进阶内容/GamePlay系统/怪物系统/AI编辑器1.0 (id:179)


### 示例 1

```lua
local AIPlayerKey = UGCFakePlayerSystem.GetRandomAIPlayerKey()
UGCFakePlayerSystem.SpawnFakePlayer(AIPlayerKey)
```


## 新建技能阶段，新建技能Action

> 来源: 进阶内容/GamePlay系统/技能系统/技能编辑器1.0 (id:182)


### 示例 1

```lua
--获取技能管理器
local SkillManager = self:GetSkillManagerComponent()
if SkillManager ~= nil then
	--获得当前正在运行的技能的Index
	local SkillIndex = SkillManager:GetCurSkillIndex()
	--向此技能发送SkillCancel事件
	SkillManager:TriggerEvent(SkillIndex, UTSkillEventType.SET_SKILL_CANCEL)
end
```


### 示例 2

```lua
--获取技能管理器
local SkillManager = PlayerPawn:GetSkillManagerComponent()
if SkillManager ~= nil then
	--获得当前正在运行的技能的Index
	local SkillIndex = SkillManager:GetCurSkillIndex()
	--向此技能发送自定义事件
	SkillManager:TriggerStringEvent(SkillID, “MySkillEvent”)
end
```


### 示例 3

```lua
--获取技能管理器
local SkillManager = self:GetSkillManagerComponent()
if SkillManager ~= nil then
	--获得当前正在运行的技能的Index
	local SkillIndex = SkillManager:GetCurSkillIndex()
	--向此技能发送PickItem事件
	SkillManager:TriggerCurSkillEvent(EUAESkillEvent.PickItem, SkillIndex)
end
```


## 自定义自己的技能Action节点

> 来源: 进阶内容/GamePlay系统/技能系统/技能编辑器1.0 (id:183)


### 示例 1

```lua
--技能执行完毕调用
function mySkillAction:OnReset()
    print("mySkillAction:OnReset")
    return true
end

--Action每一帧调用
function mySkillAction:OnUpdateAction(DeltaSeconds)
    print("mySkillAction:OnUpdateAction")
    return true
end

--Action开始执行调用
function mySkillAction:OnRealDoAction()
    print("mySkillAction:OnRealDoAction")
    return true
end
```


### 示例 2

```lua
--获取技能释放者Pawn
--进而获取PlayerKey
--其他定义在Pawn的属性也可以获取
local SkillPawn = self:GetOwnerPawn()
print(SkillPawn.PlayerKey)
```


## 为玩家添加初始道具

> 来源: 进阶内容/GamePlay系统/物资系统/物资编辑器1.0 (id:193)


### 示例 1

```lua
---@class UGCPlayerController_C:BP_STExtraPlayerController_C
--Edit Below--
local UGCPlayerController = {}; 

-- function UGCPlayerController:ReceiveBeginPlay()

-- end
-- function UGCPlayerController:ReceiveTick(DeltaTime)

-- end
-- function UGCPlayerController:ReceiveEndPlay()
 
-- end
return UGCPlayerController;
```


### 示例 2

```lua
local UGCPlayerController = {}; 

    function UGCPlayerController:ReceiveBeginPlay()
		-- 执行父类的ReceiveBeginPlay函数
        self.SuperClass.ReceiveBeginPlay(self)
		-- 创建一个延时执行函数体
        local OBTimerDelegate = ObjectExtend.CreateDelegate(self, 
        function()
        if self:HasAuthority() == true then -- 判断是否在服务端执行
            local PlayerPawn = self:GetPlayerCharacterSafety()  -- 获取玩家角色Pawn
            UGCBackPackSystem.AddItem(PlayerPawn, 102005, 1)  -- 为角色增加物品
        end
        end
        )
		-- 执行延时函数体
        KismetSystemLibrary.K2_SetTimerDelegateForLua(OBTimerDelegate, self, 2, false)
    end
-- function UGCPlayerController:ReceiveTick(DeltaTime)

-- end
-- function UGCPlayerController:ReceiveEndPlay()
 
-- end
return UGCPlayerController;
```


### 示例 3

```lua
-- 为玩家添加物品
-- @param PlayerPawn  玩家Pawn
-- @param ItemID  物品ID
-- @param Num  物品数量
UGCBackPackSystem.AddItem(PlayerPawn, ItemID, Num)
```


## 物资刷新示例

> 来源: 进阶内容/GamePlay系统/物资系统/物资编辑器1.0 (id:194)


### 示例 1

```lua
DropItemConfig = require("Script.DropItemConfig")
UGCDropItemMgr = require("Script.UGCDropItemMgr")
local SpawnActor = {}; 
function SpawnActor:ReceiveBeginPlay()
---检测是否在服务端执行
    if not self:HasAuthority() then
        self:K2_DestroyActor()
        return
    end
---生成物资
    local ItemList = DropItemConfig.GetDropItems(DropItemConfig.ItemKey.RandomItem, self.DropItemGroup)
    UGCDropItemMgr.SpawnItems(ItemList, self:K2_GetActorLocation(), self:K2_GetActorRotation(), 0)
end
return SpawnActor;
```


### 示例 2

```lua
---@class ItemActor_C:SpawnActor_C
--Edit Below--
require("Script.TableHelper")
local ItemActor = TableHelper.DeepCopy(require("Script.Blueprint.SpawnActor"))
return ItemActor
```


### 示例 3

```lua
---初始化物品类型组
local function InitGroup(GroupName, GroupId)
    DropItemConfig.ItemsGroup[GroupName][GroupId] = {}
    return DropItemConfig.ItemsGroup[GroupName][GroupId]
end

---增加一个物品组
local function AddItemGroup(Data, Pro, Count)
    local Tmp = {Pro = Pro, Count = Count,Items = {}}
    table.insert(Data, Tmp)
    return Tmp
end

---增加附加子表
local function AddConcomitants(Data, ItemID, StackCount, CountMin, CountMax)
    local Tmp = {ItemID = ItemID; StackCount = StackCount; CountMin = CountMin; CountMax = CountMax}
    table.insert(Data.Concomitants, Tmp)
    return Tmp
end

--增加物品
local function AddItem(Data, ItemID , Count, Weigth, ...)
    local Tmp = {ItemID = ItemID; Count = Count; Weigth = Weigth; Concomitants = {}}

    --初始化附加子表物品
    local Concomitants = {...}
    for k,v in pairs(Concomitants) do
        local ItemID = v[1]
        local StackCount = v[2]
        local CountMin = v[3]
        local CountMax = v[4]
        AddConcomitants(Tmp, ItemID, StackCount, CountMin, CountMax)
    end
    table.insert(Data.Items, Tmp)
    return Tmp
end
```


### 示例 4

```lua
---RandomItem类型物品组配置示例
local Data = InitGroup(DropItemConfig.ItemKey.RandomItem, 25)
local ItemGroup = AddItemGroup(Data, 100 ,1)
AddItem(ItemGroup, 102001, 1, 10, {301001, 30, 1, 2})
AddItem(ItemGroup, 102005, 1, 10, {301001, 30, 1, 2})
AddItem(ItemGroup, 102003, 1, 10, {301001, 30, 1, 2})
AddItem(ItemGroup, 102002, 1, 10, {305001, 30, 1, 2})
```


## Buff简介

> 来源: 进阶内容/GamePlay系统/技能系统/Buff编辑器1.0 (id:195)


### 示例 1

```lua
--判断是否拥有此Buff
HasBuff:fun(BuffName:FName):bool

--移除Buff，可指定只移除层数
RemoveBuff:fun(BuffName:FName,RemoveLayerOnly:bool,BuffApplierActor:AActor):bool

--添加Buff，可指定来源以及添加层数
AddBuff:fun(BuffName:FName,BuffCauser:AController,LayerCount:int32,BuffApplierActor:AActor,CauserActor:AActor):int32
```


### 示例 2

```lua
PlayerPawn:AddBuff("MyBuff", nil, 1, nil, nil)
```


## 自定义自己的BuffAction

> 来源: 进阶内容/GamePlay系统/技能系统/Buff编辑器1.0 (id:196)


### 示例 1

```lua
local myBuffAction = {}; 

function myBuffAction:LuaDoAction()
    print("myBuffAction:LuaDoAction")
    return true
end

function myBuffAction:LuaUndoAction()
    print("myBuffAction:LuaUndoAction")
    return true
end

function myBuffAction:LuaResetAction()
    print("myBuffAction:LuaResetAction")
    return true
end

function myBuffAction:LuaUpdateAction(DeltaSeconds)
    print("myBuffAction:LuaUpdateAction:"..DeltaSeconds)
    return true
end


return myBuffAction;
```


## 网络同步系统介绍

> 来源: 进阶内容 (id:203)


### 示例 1

```lua
local PeekabooPlayerController = 
{
    IsTransform = false;
    TransformTypes = {};
}
function PeekabooPlayerController:GetReplicatedProperties()
    return 
    "IsTransform",
    "TransformTypes"
end
```


### 示例 2

```lua
function PeekabooPlayerController:OnRep_IsTransform()
    print(string.format("PeekabooPlayerController:OnRep_IsTransform[%s] IsTransform[%s]", self.PlayerName, tostring(self.IsTransform)));

    UGCEventSystem:SendEvent(PeekabooModeEventType.IsTransformChange, self, self.IsTransform);
end

function PeekabooPlayerController:OnRep_TransformTypes()
    print(string.format("PeekabooPlayerController:OnRep_TransformTypes[%s]", self.PlayerName));
    log_tree("TransformTypes:",self.TransformTypes);

    UGCEventSystem:SendEvent(PeekabooModeEventType.TransformTypesChange, self, self.TransformTypes);
end
```


## 自定义面板

> 来源: 进阶内容/和平精英集成功能 (id:204)


### 示例 1

```lua
---把自定义UI挂到和平UI上并应用自定义布局
---生效范围：C
---@param Widget Widget*
function UGCWidgetManagerSystem.AddChildToTochButton(Widget)
```


### 示例 2

```lua
function UGCWidgetManagerSystem.AddChildToTochButton(Widget)
```


### 示例 3

```lua
---@class UGCGameState_C:BP_UGCGameState_C
--Edit Below--
local UGCGameState = {}; 
UGCGameState.MainUIClassPath = UGCMapInfoLib.GetRootLongPackagePath().. "Asset/testMainUI.testMainUI_C";     --test按钮所在控件蓝图路径MainUIClassPath
 function UGCGameState:ReceiveBeginPlay()
    local PlayerController = GameplayStatics.GetPlayerController(self, 0); --拿到客户端的PlayerController

    local MainUIClass = UE.LoadClass(UGCGameState.MainUIClassPath);  --加载MainUIClassPath的类
    local MainUI = UserWidget.NewWidgetObjectBP(PlayerController, MainUIClass); --新建MainUIClass的对象
    --MainUI:AddToViewport();
    UGCWidgetManagerSystem.AddChildToTochButton(MainUI); --关键函数，应用到和平主界面上并且读取自定义面板中玩家设置的UI信息
 end
-- function UGCGameState:ReceiveTick(DeltaTime)

-- end
-- function UGCGameState:ReceiveEndPlay()
 
-- end
return UGCGameState;
```


## 空投系统1.0

> 来源: 进阶内容/和平精英集成功能/旧版本功能 (id:207)


### 示例 1

```lua
local gamemode = UGCGameSystem.GameMode;
    -- Get gamemode

    local aircraft = gamemode.TransportAircraft;
    -- Get aircraft component

    aircraft.DropLoc = Vector.New(21440.0,28050.0,150);
    -- Set Drop Location
    
    aircraft.InStringPath = UGCMapInfoLib.GetRootLongPackagePath() .. "Asset/Blueprint/Generator.Generator_C";
    -- Set Aircraft Generator

    aircraft:ExecuteFlightPlan();
```


## 全局（房间）观战

> 来源: 进阶内容/和平精英集成功能/观战功能 (id:210)


### 示例 1

```lua
local Mgr = GameFrontendHUD:GetLogicManagerByName("observe")
if mgr then
      local OBUI = InGameUIManager.GetWidgetFromName(Mgr, "OBUI_UIBP")
end
```


### 示例 2

```lua
local ViewTarget = Controller:GetViewTarget()
ViewTarget:SetActorLocation({X=新位置x, Y=新位置y, Z=新位置z })
```


## 语音系统

> 来源: 进阶内容/和平精英集成功能 (id:212)


### 示例 1

```lua
function UGCPlayerController:OnRep_UGCTeamID()				--UGCTeamID为开发者自己注册的同步参数
    local RoomName = "TeamVoiceRoom"..tostring(self.UGCTeamID)
    UGCVoiceManagerSystem.JoinVoiceRoom(RoomName)
end
```


## 玩家头像

> 来源: 进阶内容/和平精英集成功能 (id:214)


### 示例 1

```lua
Common_Avatar_BP_Widget:InitView(
int Style,
int UID,
string IconURL, 
int Gender,
int FrameLevel,
int PlayerLevel,
bool IgnoreFrame,
bool IsMySelf)
```


## 开启/禁用所有伤害

> 来源: 常用实战教学 (id:236)


### 示例 1

```lua
local Action_SetEnableDamage = 
{
	IsEnableDamage = false;
}


function Action_SetEnableDamage:Execute()
	ugcprint(string.format("Action_SetEnableDamage:Execute IsEnableDamage[%s]", tostring(self.IsEnableDamage)));

	UGCGameSystem.GameMode.bEnableDamage = self.IsEnableDamage;

	return true;
end


return Action_SetEnableDamage
```


## 使用同模角色优化性能

> 来源: 性能优化及异常处理 (id:263)


### 示例 1

```lua
local PlayerPawn = ScriptGameplayStatics.GetCharacterByPlayerKey(self, PlayerKey);
	local AvatarComponent = PlayerPawn:getAvatarComponent();
	AvatarComponent:InitDefaultAvatarByResID(0, 401991, 0)
```


## 异步加载

> 来源: 性能优化及异常处理 (id:267)


### 示例 1

```lua
if UGCAsyncLoadTools ~= nil then
        UGCAsyncLoadTools:LoadObject(MeshPath, function(Asset)
            if self ~= nil and UE.IsValid(self) then
                self.Sphere:SetStaticMesh(Asset);
            end
        end)
    end
```


## 弱网，断线重连处理

> 来源: 性能优化及异常处理 (id:270)


### 示例 1

```lua
--在此函数中返回的变量为属性同步变量
function MyMonster:GetReplicatedProperties()
    return "Health"
end

--OnRep_变量名为客户端受到变量同步后，响应的函数，此函数被调用时，变量已是同步后的值
function MyMonster:OnRep_Health(PrevHealth)
    print("MyMonster:OnRep_Health")
end
```


## 游戏结束时的逻辑处理

> 来源: 绿洲编辑器基础内容/脚本编写 (id:272)


### 示例 1

```lua
local RemainTime = UGCGameSystem.GetDSRemainingTime()
print(string.format("当前DS剩余关闭时间：%s 秒", RemainTime))
```


### 示例 2

```lua
-- 注册通知点，离关闭前60秒将触发事件
UGCGameSystem.SetDSCloseNotify({60})

-- 监听关闭通知事件，执行数据存档
UGCGenericMessageSystem.ListenGlobalMessage(
	UGCGameSystem.GameState,
	UGCGenericMessageSystem.UserDefinedMessages.UGC.UGCDSShutDownManager.DSCloseNotify,
	self,
	function()
		UGCPlayerStateSystem.SavePlayerArchiveData(UID, self.PlayerData)
  end
)
```


## 超大地图关卡加载方式

> 来源: 新手入门/游戏场景/关卡编辑 (id:279)


### 示例 1

```lua
local LoadLevel = {}

function LoadLevel:ReceiveBeginPlay()
    ugcprint("LoadLevel:ReceiveBeginPlay");
    if self:HasAuthority() == true then
        self.Box.OnComponentBeginOverlap:Add(self.OnBeginOverlap, self);
        self.Box.OnComponentEndOverlap:Add(self.OnEndOverlap, self);
    end
end


function LoadLevel:OnBeginOverlap()
    GameplayStatics.LoadStreamLevel(self, "NewMap01", true, false)
end

function LoadLevel:OnEndOverlap()
    GameplayStatics.UnloadStreamLevel(self, "NewMap01")
end

-- end
-- function LoadLevel:ReceiveTick(DeltaTime)

-- end
-- function LoadLevel:ReceiveEndPlay()
 
-- end
return LoadLevel;
```


## 制作HellowWorld工程

> 来源: 新手入门/实现HelloWord (id:298)


### 示例 1

```lua
local MyHelloWorld = {}; 
--近似构造函数，客户端，战斗服务器运行
-- function MyHelloWorld:ReceiveBeginPlay()
-- end
	
--每帧都执行，客户端，战斗服务器运行
--DeltaTime为上帧到本帧执行的时间
-- function MyHelloWorld:ReceiveTick(DeltaTime)
-- end
	
--近似析构函数，客户端，战斗服务器运行
-- function MyHelloWorld:ReceiveEndPlay()
-- end
	
return MyHelloWorld;
```


### 示例 2

```lua
function UGCGameMode:ReceiveBeginPlay()
		--仅在服务器运行
		if self:HasAuthority() then 
        --根据路径加载类
        local Path_Hello = UGCMapInfoLib.GetRootLongPackagePath().."Asset/Blueprint/MyHelloWorld.MyHelloWorld_C"
        local Class_Hello = UE.LoadClass(Path_Hello)

        --刷出Actor
        local BP_Hello = ScriptGameplayStatics.SpawnActor(self, Class_Hello, 
        {X = 21520, Y = 28980, Z = 150},    --坐标
        {Roll = 0, Pitch = 0, Yaw = 0},     --旋转
        {X = 1, Y = 1, Z = 1})              --缩放
    end
end
```


### 示例 3

```lua
local MyHelloWorld = 
{
    TestValue = 0;
};

function MyHelloWorld:ReceiveBeginPlay()
    if self:HasAuthority() then
        --仅在战斗服务器运行
        self.TestValue = 1
        print("Log HelloWorld: ReceiveBeginPlay Server:"..self.TestValue)

        self.Box.OnComponentBeginOverlap:Add(self.OnBeginOverlap, self);
    else
        --仅在客户端运行
        print("Log HelloWorld: ReceiveBeginPlay Client:"..self.TestValue)
    end
end
```


### 示例 4

```lua
--标记属性为同步
function MyHelloWorld:GetReplicatedProperties()
    return "TestValue"
end

--战斗服务器修改后，自动同步到客户端
--函数名规则OnRep_变量名
function MyHelloWorld:OnRep_TestValue()
    print("Log HelloWorld:"..self.TestValue)
end
```


### 示例 5

```lua
--返回多个变量
function MyHelloWorld:GetReplicatedProperties()
    return "TestValue","NewValue"
end
```


## 经典海岛模板

> 来源: 玩法案例及模板 (id:302)


### 示例 1

```lua
--[[------------------------------------------激活游戏状态控制------------------------------------------------------]]--

local Action_GameActiveCheck = {}

function Action_GameActiveCheck:Execute()
	print("Action_GameActiveCheck:Execute " ..tostring(self))
	
	UGCBlueprintFunctionLibrary.InitGameModeStates();
	UGCGameSystem.GameState.GameModeState = "FightingState"
	
	return true;
end

return Action_GameActiveCheck;
```


### 示例 2

```lua
local Action_TogglePoisonCircle = {}
function Action_TogglePoisonCircle:Execute(...)
	print("Action_TogglePoisonCircle:Execute " ..tostring(self));
	UGCBlueprintFunctionLibrary.TogglePoisonCircle(self, true);
	return false
end
function Action_TogglePoisonCircle:Update(deltaTime)
end
return Action_TogglePoisonCircle
```


## 使用枚举类型

> 来源: 进阶内容 (id:339)


### 示例 1

```lua
--Edit Below--
local MyActor = {
    MyEnumration = MyEnum.Banana
};
```


### 示例 2

```lua
---@enum MyEnum
MyEnum = { 
    Apple = 0,
    Orange = 1,
    Banana = 2,
};
```


### 示例 3

```lua
function MyActor:ReceiveBeginPlay()
    print("MyActor:ReceiveBeginPlay");
    print(MyEnum.Apple); -- 输出的值为0
end
```


### 示例 4

```lua
---@class MyActor_C:Actor
---@field Box UBoxComponent
---@field DefaultSceneRoot USceneComponent
---@field MyEnumeration TEnumAsByte<MyEnum>

require('Script.Common.ue_enum_custom') -- 2022年6月版本无需手动require

--Edit Below--
local MyActor = {}; 

function MyActor:ReceiveBeginPlay()
    print("MyActor:ReceiveBeginPlay");
    print(MyEnum.Apple); -- 输出的值为0
    print(self.MyEnumeration); -- 输出的值与MyActor蓝图中的枚举值一致
    if self:HasAuthority() == true then
        --仅在服务器运行
        self.Box.OnComponentBeginOverlap:Add(self.BeginOverlap, self);
        self.Box.OnComponentEndOverlap:Add(self.EndOverlap, self);
    end

end

function MyActor:BeginOverlap()
    print("MyActor:BeginOverlap");
    self.MyEnumeration = MyEnum.Banana; -- 输出的值为2
    print(self.MyEnumeration);
end

function MyActor:EndOverlap()
    print("MyActor:EndOverlap");
    self.MyEnumeration = MyEnum.Orange; -- 输出的值为1
    print(self.MyEnumeration);
end
-- function MyActor:ReceiveTick(DeltaTime)

-- end
-- function MyActor:ReceiveEndPlay()
 
-- end
return MyActor;
```


## 绿洲启元Lua脚本开发指南

> 来源: 绿洲编辑器基础内容/脚本编写 (id:346)


### 示例 1

```lua
-- 类静态方法调用,大部分UE4全局类，如UGameplayStatics都可以直接把前缀删除后直接使用。
local PlayerPawn = GameplayStatics.GetPlayerCharacter(self,0) 
-- 对象属性访问，可以访问该对象C++/蓝图/Lua属性。
local PlayerName = PlayerPawn.PlayerName	
-- 对象属性修改，可以修改该对象C++/蓝图/Lua属性。
PlayerPawn.PlayerName = "Test"
-- 对象方法调用：使用默认值参数。
PlayerPawn:K2_SetActorLocation(Vector(0,0,0)) 	
--PS：由于C++的默认参数是编译期生效，Lua脚本是运行时生效，所以无法直接使用C++接口默认值，这里的默认值使用0x0，number型为0，bool类型为fasle，指针为nil，如果是结构体那么内部参数按照以上默认值规则。
```


### 示例 2

```lua
local UGCGameMode = {}; 
function UGCGameMode:ReceiveBeginPlay()
    self.SuperClass.ReceiveBeginPlay(self)	--作用等同于C++的Super::ReceiveBeginPlay()
end
```


### 示例 3

```lua
local Widget  = CreateUIWidget(bp_lua, "/Game/Demo/DemoUI.DemoUI_C");
Widget:SetVisibility(ESlateVisibility.Visible);
```


### 示例 4

```lua
local object = CreateUIWidget(bp_lua, "/Game/Demo/DemoUI.DemoUI_C");
local t1 = object.ColorAndOpacity;
local t2 = object.ColorAndOpacity.R;
local t3 = t1.R;

print(type(t1));        -- userdata

object.ColorAndOpacity = t1;                    -- 使用 userdata 类型赋值;
object.ColorAndOpacity = {R=1, G=2, B=3, A=1};  -- 使用table类型赋值; = FLinearColor(1,2,3,1);
object.ColorAndOpacity = {R=1, A=1};            -- 使用table类型赋值; = FLinearColor(1,0,0,1);
```


### 示例 5

```lua
local object = CreateUIWidget(bp_lua, "/Game/Demo/DemoUI.DemoUI_C");
-- DemoUI 中存在数据成员：TArray<int32> BPVarArrayInt;
-- DemoUI 中存在数据成员：TSet<int32> BPVarSetInt;
-- DemoUI 中存在数据成员：TMap<int32, FString> BPVarMapIntStr;

local v1 = object.BPVarArrayInt:Num();   --用#object.BPVarArrayInt亦可

-- 成员访问：示例忽略越界考虑, 仅做语法说明;
local v11 = object.BPVarArrayInt[2];    
local v12 = object.BPVarSetInt[3];
local v13 = object.BPVarMapIntStr[233];

-- 成员修改:
object.BPVarArrayInt:Add(234);
object.BPVarSetInt:Add(234);
object.BPVarMapIntStr:Add(234, "abc");

-- 遍历
for k,v in pairs(object.BPVarArrayInt) do
    print(k, v);    -- k为索引[1,N]; v为数据成员;
end
for k,v in pairs(object.BPVarSetInt) do
    print(k, v);    -- k为索引[1,N]; v为数据成员;
end
for k,v in pairs(object.BPVarMapIntStr) do
    print(k, v);    -- k为Iter.Key; v为Iter.Value;
end
```


### 示例 6

```lua
---@class MainUI_C:UserWidget   
---@field TestButton UButton            --该UMG有一个TestButton的按钮控件
local MainUI = {}; 
function MainUI:Construct()
    self.TestButton.OnMouseButtonDownEvent:Bind(self.TestButton_OnMouseButtonDown, self)   --绑定TestButton_OnMouseButtonDown函数
    self.TestButton.OnMouseButtonDownEvent:Bind(                                           --绑定匿名函数，单播委托此处绑定会直接覆盖上次绑定，这里仅做示范
        function(MyGeometry,MouseEvent) 
            print("MainUI:TestButton_OnMouseButtonDown")
        end, self)  

    self.TestButton.OnMouseButtonDownEvent:UnBind()                     -- 移除绑定
    self.TestButton.OnMouseButtonDownEvent:Execute(nil, nil)            -- 触发代理（控件事件不需要主动触发,此仅做用法示例）

    self.TestButton.OnClicked:Add(self.TestButton_OnClicked, self)   --绑定TestButton_OnClicked函数
    self.TestButton.OnClicked:Add(                                   --绑定匿名函数，注意绑定匿名函数无法通过Remove移除绑定
        function() 
            print("MainUI:TestButton_OnClicked")
        end, self)  

    self.TestButton.OnClicked:AddInstance(self.TestButton_OnClicked, self)  --此次绑定由于是同名同参，会被覆盖
    self.TestButton.OnClicked:Remove(self.TestButton_OnClicked, self)       --移除绑定
    self.TestButton.OnClicked:RemoveAll()                                   --移除所有绑定
    self.TestButton.OnClicked:Broadcast()                                   --触发广播代理（控件事件不需要主动触发,此仅做用法示例）

    self.TimerDelegate = ObjectExtend.CreateDelegate(self,                  --创建Delegate
        function()
            print("TimerExecute")
            ObjectExtend.DestroyDelegate(self.TimerDelegate)                --触发后可以手动销毁
        end
    )
    self.TimerHandle = KismetSystemLibrary.K2_SetTimerDelegateForLua(self.TimerDelegate, self, 3, false)    --执行Timer，定时3秒，不循环
end

function MainUI:TestButton_OnMouseButtonDown(MyGeometry,MouseEvent)
    print("MainUI:TestButton_OnMouseButtonDown")
end

function MainUI:TestButton_OnClicked()
    print("MainUI:TestButton_OnClicked")
end
```


### 示例 7

```lua
-- lua
local DemoWidget = CreateUIWidget(bp_lua, "/Game/Demo/DemoWidget.DemoWidget_C");

local Param = DemoWidget:GetParam();
local ParamRef = DemoWidget:GetParamRef();      -- 等价与 GetParam 调用;
ParamRef = 123;   -- 声明引用也是拷贝形式返回：赋值只更新lua变量值, 不会更新DemoWidget中的Param;

local OutParam1,OutParam2;
local Result;
Result,OutParam1,OutParam2 = DemoWidget:GetDoubleParam(OutParam1,OutParam2)	 --非const引用参数会作为返回结果输出,依次位于return值后面
```


### 示例 8

```lua
local Actor = SpawnActor(...)
Actor:K2_DestroyActor()
--这里建议销毁后手动设置Actor = nil
if Actor == nil then end   --Actor不为nil
if UE.IsValid(Actor) == false then end  --UE.IsValid(Actor)为false
```


### 示例 9

```lua
CPPFunction();--这里会使用0x0的值，也就是Param会是0
CPPFunction(1); --√
```


### 示例 10

```lua
void CPPFunction(Vector Param);
......
CPPFunction({X=1,Y=2,Z=3});		--×
CPPFunction(Vector.New(1,2,3));	--√
```


### 示例 11

```lua
function BP_Worship:RecordBeginAreaPlayer(CheckActorArray)
	self.TotalBeginAreaPlayer = CheckActorArray.OverlapActorList:Copy()
end
```


## 调试日志说明

> 来源: 绿洲编辑器基础内容/调试游戏 (id:354)


### 示例 1

```lua
function TestCube:ReceiveBeginPlay()
    ugcprint("TestCube:ReceiveBeginPlay")
    self.SuperClass.ReceiveBeginPlay(self);
    self.Box.OnComponentBeginOverlap:Add(self.Box_OnComponentBeginOverlap,self)
end

function TestCube:Box_OnComponentBeginOverlap(OverlappedComponent, OtherActor, OtherComp, OtherBodyIndex, bFromSweep, SweepResult)
    ugcprint("TestCube:Box_OnComponentBeginOverlap")
end
```


### 示例 2

```lua
function TestCube:Box_OnComponentBeginOverlap(OverlappedComponent, OtherActor, OtherComp, OtherBodyIndex, bFromSweep, SweepResult)
    ugcprint("TestCube:Box_OnComponentBeginOverlap")
    ugcprint("TestCube:Box_OnComponentBeginOverlap:"..UE.GetFullName(OverlappedComponent))
    ugcprint("TestCube:Box_OnComponentBeginOverlap:"..UE.GetFullName(OtherActor))
end
```


### 示例 3

```lua
UGCServerLog: TestCube:Box_OnComponentBeginOverlap:BoxComponent /ColorChange/UGCmap.UGCmap:PersistentLevel.TestCube_14.Box
UGCServerLog: TestCube:Box_OnComponentBeginOverlap:UGCPlayerPawn_C /ColorChange/UGCmap.UGCmap:PersistentLevel.UGCPlayerPawn_C_0
```


### 示例 4

```lua
function TestCube:Box_OnComponentBeginOverlap(OverlappedComponent, OtherActor, OtherComp, OtherBodyIndex, bFromSweep, SweepResult)
    --较为繁琐的日志打印方式
    ugcprint("TestCube:Box_OnComponentBeginOverlap")
    ugcprint("TestCube:Box_OnComponentBeginOverlap:"..UE.GetFullName(OverlappedComponent))
    ugcprint("TestCube:Box_OnComponentBeginOverlap:"..UE.GetFullName(OtherActor))

    --封装工具简化日志打印
    UGCLog.Log("TestCube:Box_OnComponentBeginOverlap")
    --各种内容混排,无需手动拼接字符串
    UGCLog.Log(OtherActor, OverlappedComponent)
    UGCLog.Log("TestCube:Box_OnComponentBeginOverlap", 123, {x=1, y="haha", z= {v = OtherActor}}, OtherActor)
    --空对象传入也不会影响
    local value = nil
    UGCLog.Log("TestCube:Box_OnComponentBeginOverlap test nil", value, 123)
end
```


### 示例 5

```lua
UGCLog = UGCLog or {}


UGCLog.IsServerCached = nil;

function UGCLog.IsServer()
    if UGCLog.IsServerCached == nil then
        UGCLog.IsServerCached = UGCGameSystem.IsServer();
    end

    return UGCLog.IsServerCached;
end


local function tostringex(v, len)
    if len == nil then len = 0 end
    local pre = string.rep('\t', len)
    local ret = ""
    if type(v) == "table" then
        if len > 5 then return "\t{ ... }" end
        local t = ""
        local keys = {}
        for k, v1 in pairs(v) do
            table.insert(keys, k)
        end
        
        for k, v1 in pairs(keys) do
            k = v1
            v1 = v[k]
            t = t .. "\n\t" .. pre .. tostring(k) .. ":"
            t = t .. tostringex(v1, len + 1)
        end

        if t == "" then
            ret = ret .. pre .. "\n{ }\t(" .. tostring(v) .. ")"
        else
            if len > 0 then
                ret = ret .. "\t(" .. tostring(v) .. ")\n"  
            else
                ret = ret .. "\n(" .. tostring(v) .. ")"    
            end

            if len == 0 then
                ret = ret .. "\n" .. pre .. "{" .. t .. "\n" .. pre .. "}"  
            else
                ret = ret .. pre .. "{" .. t .. "\n" .. pre .. "}"  
            end
        end
    elseif type(v) == "userdata" then
        if UE.IsValid(v) then
            local Nextpre = string.rep('\t', len+1)
            ret = ret .. pre .. tostring(UE.ToTable(v)) .. "\t(" .. type(v) .. ")"
            ret = ret .. "\n" .. pre .. "{\n" .. Nextpre .. "ClassAndPath:\t" .. UE.GetFullName(v) .. "\n" .. pre .. "}"
        elseif v ~= nil then
            ret = ret .. pre .. tostring(UE.ToTable(v)) .. "\t(" .. type(v) .. ")"
        else
            ret = ret .. pre .. "[NullActor]"
        end
        
    else
        ret = ret .. pre .. tostring(v) .. "\t(" .. type(v) .. ")"
    end
    return ret
end

local function GetName(Actor)
    if UE.IsValid(Actor) then
        return string.format("\n%s:\n{\n ClassAndPath:\t%s\n}", tostring(UE.ToTable(Actor)), UE.GetFullName(Actor))
    elseif Actor ~= nil then
        return type(Actor)
    end

    return "[NullActor]";
end

local LogTag = function()
    local GameStr = "[UGC]";

    if UGCLog.IsServer() then
        GameStr = GameStr .. "[服务器]"
    else
        GameStr = GameStr .. "[客户端]"
    end

    return GameStr;
end

local GetLogStr = function(...)
    local data = {}
    local index = 1
    for k, v in pairs({...}) do
        if k > index + 1 then
            data[k - 1] = "nil"
        end

        local typev = type(v)
        if typev == "table" then
            data[k] = tostringex(v)
        elseif typev == "userdata" then
            data[k] = GetName(v)
        else
            data[k] = tostring(v)
        end

        index = k
    end

    local out = table.concat(data, "\t")
    if out:len() >= 10 * 1024 - 100 then
        out = out:sub(1, 10 * 1024 - 100) .. "..."
    end

    return out
end

local BaseLog = function(...)
    --优化，判断开关，开关状态打开才进行字符串序列化
    if UGC_DEV_LOG then
        if _G.bEnableUGCDebugLog == nil or _G.bEnableUGCDebugLog > 0 then
            ugcprint(LogTag() .. GetLogStr(...))
        end
    end
end

local BaseLogShipping = function(tag, ...)
    print(LogTag() .. tag .. GetLogStr(...))
end

function UGCLog.LogIf(Cond, ...)
    if Cond then
        BaseLog(...)
    end
end

function UGCLog.LogErrorIf(Cond, ...)
    if Cond then
        BaseLogShipping("[Error]", ...)
    end
end

function UGCLog.Log(...)
    BaseLog(...)
end

function UGCLog.LogError(...)
    BaseLogShipping("[Error]", ...)
end

function UGCLog.LogUEObj(Obj)
    UGCLog.Log("[UEObj]", UE.ToTable(Obj))
end

function UGCLog.LogUEObjList(Lst)
    print("[UGC][UEObj] UGCLog.LogUEObjList");
    for k, v in pairs(Lst) do
        UGCLog.Log("[UEObj]", GetName(v))
    end
end

function UGCLog.LogUEPawnList(Lst)
    local TableHelper = require("Script.Common.TableHelper");
    for k, v in pairs(Lst) do
        UGCLog.Log("[Pawn]", v.PlayerKey, GetName(v))
    end
end

function UGCLog.LogNetStatus(PlayerKey)    
    local PS = UGCGameSystem.GetPlayerStateByPlayerKey(PlayerKey)

    if PS == nil then
        UGCLog.Log("[Net]还没有还没有PlayerState:", PlayerKey)
        return;
    end

    UGCLog.Log("[Net]PlayerKey:", PlayerKey, "是否断线：", PS.isLostConnection, "是否重连：", PS.isReconnected)
end

UGCLog.GameFlowStep = 1;
function UGCLog.LogGameFlow(...)
    BaseLogShipping("[GameFlow]", "["..UGCLog.GameFlowStep.."]", ...)
    UGCLog.GameFlowStep = UGCLog.GameFlowStep + 1;
end

return UGCLog;
```


## 角色出生点

> 来源: 进阶内容/GamePlay系统/角色系统 (id:362)


### 示例 1

```lua
--角色出生点管理器
local MyStartManager = {}; 

function MyStartManager:GetUGCModePlayerStart(Controller)

    --标记当前属于服务器还是客户端
    if UGCGameSystem.GameState:HasAuthority() == true then 
        print("MMG_Lua MyStartManager:GetUGCModePlayerStart Server");
    else
        print("MMG_Lua MyStartManager:GetUGCModePlayerStart Client");
    end

    --获取当前控制器的玩家状态（PlayerState）
    local PlayerState = Controller.PlayerState;

    --PlayerState为空时，打印警告
    if PlayerState == nil then
        print( "Error: MyStartManager:GetUGCModePlayerStart PlayerState is nil!");
    end

    --根据玩家团队编号获取玩家出生点（PlayerStart）
    local SelectedPlayerStart = self:FindPlayerStartByBornPointID(PlayerState.TeamID, true);--当一个出生点已经被使用过一次，用于出生玩家时，自动寻找其他未被使用的出生点

    --打印当前玩家的队伍编号（TeamID）
    print(string.format("MMG_Lua MyStartManager:GetUGCModePlayerStart PlayerState.TeamID[%s]", PlayerState.TeamID));

    --若找到玩家出生点（PlayerStart），打印当前玩家出生点名称和对应BornID，否则输出错误信息
    if SelectedPlayerStart ~= nil then
        print(string.format("MyStartManager:GetUGCModePlayerStart SelectedPlayerStart[%s] BornID[%d] PlayerID[%s]",   
        KismetSystemLibrary.GetObjectName(SelectedPlayerStart), SelectedPlayerStart.PlayerBornPointID, Controller.PlayerKey));
        --设置当前PlayerStart被占用
        SelectedPlayerStart:SetMarkOccupied();
        print(string.format( "PlayerStartOccupied?[%s]",SelectedPlayerStart:IsMarkOccupied()));
        return SelectedPlayerStart;
    else
        print("Error: MyStartManager:GetUGCModePlayerStart SelectedPlayerStart is nil!");
    end

    return nil;
end

return MyStartManager;
```


## 绿洲启元段位和赛季接入指南

> 来源: 进阶内容/和平精英集成功能 (id:374)


### 示例 1

```lua
---查询段位分
---生效范围：S
---@return UGCRankRate float @实际伤害
---@param Count int  @段位分
function UGCRankSystem.GetUGCRank(PlayerKey)
end
```


### 示例 2

```lua
---修改段位分
---生效范围：S
---@param PlayerKey int  @玩家PlayerKey
---@param Count int  @段位分
function UGCRankSystem.AddRanktProgress(PlayerKey, Count)
end
```


### 示例 3

```lua
---重置新赛季玩家段位分
---生效范围：S
---@param GameSeasonId int  @当前玩法的赛季Id
---@param PlayerSeasonId int  @玩家账户所处的赛季Id
---@param PlayerRank int  @玩家账户当前段位分数
---@return NewSeasonRank int  @玩家账户在新赛季重置后的段位分数
function PlayerController：GetUGCPlayerNewSeasonRankInNewGameSeason(GameSeasonId, PlayerSeasonId, PlayerRank)
    ...
    return NewSeasonRank
end
```


## 绿洲启元徽章接入指南

> 来源: 进阶内容/和平精英集成功能 (id:375)


### 示例 1

```lua
---累积徽章进度
---生效范围：S
---@param PlayerKey int  @玩家PlayerKey
---@param AchievementID int  @徽章ID
---@param Count int  @徽章计数
function UGCAchievementSystem.AddAchievementProgress(PlayerKey, AchievementID, Count)
end
```


## 二次匹配功能

> 来源: 绿洲编辑器基础内容/匹配系统 (id:376)


### 示例 1

```lua
function TestButton_UIBP:RequestCancelMatch()
    UGCMultiMode.RequestCancelMatch()
end
```


## 普通载具

> 来源: 进阶内容/GamePlay系统/载具系统 (id:379)


### 示例 1

```lua
-- 加载指定载具的蓝图类
local DaciaCarClass = UE.LoadClass('/Game/Arts_PlayerBluePrints/Vehicle/Dacia/VH_Dacia_New.VH_Dacia_New_C');

-- 将载具生成至指定位置
local carActor = ScriptGameplayStatics.SpawnActor(self, DaciaCarClass, Vector.New(22040, 30400, 100), KismetMathLibrary.MakeRotator(0, 0, 70), { X = 1, Y = 1, Z = 1 }, nil, ESpawnActorCollisionHandlingMethod.AlwaysSpawn)
```


## 商业化系统

> 来源: 商业化及功能模板 (id:20008)


### 示例 1

```lua
--绑定委托
UGCCommoditySystem.BuyUGCCommodityResultDelegate:Add(OnBuyUGCCommodityResult, Tester)

--委托解绑
UGCCommoditySystem.BuyUGCCommodityResultDelegate:Remove(OnBuyUGCCommodityResult, Tester)
```


### 示例 2

```lua
--绑定委托
UGCCommoditySystem.UseUGCCommodityResultDelegate:Add(OnUseUGCCommodityResult, Tester)

--委托解绑
UGCCommoditySystem.UseUGCCommodityResultDelegate:Remove(OnUseUGCCommodityResult, Tester)
```


### 示例 3

```lua
--在PlayerController中重载Server_OnUGCCommodityPlayerDataReady
		function UGCPlayerController:Server_OnUGCCommodityPlayerDataReady()
			UGCCommoditySystem.GetAllPlayerUGCCommodityList()
		end
```


### 示例 4

```lua
local UGCItemData = 
	{
		[UID_1] = 
		{
			[1] = 
			{
				["CommodityID"] = ...;
				["Count"] = ...;
			};
			[2] = 
			{
				["CommodityID"] = ...;
				["Count"] = ...;
			};
			...
		};
		[UID_2] = 
		{
			[1] = 
			{...
			};
			[2] = 
			{...
			};
			...
		};
	}
```


### 示例 5

```lua
local UGCItemData = 
	{
		[1] = 
		{
			["CommodityID"] = ...;
			["Count"] = ...;
		};
		[2] = 
		{
			["CommodityID"] = ...;
			["Count"] = ...;
		};
		...
	}
```


### 示例 6

```lua
local UGCItemData = 
	{
		[UID_1] = 
		{
			[ProductID1] = 
			{
				["BuyProductLimitCount"] = ...;  -- 剩余的限购数量
			};
			[ProductID2] = 
			{
				["BuyProductLimitCount"] = ...;  -- 剩余的限购数量
			};
			...
		};
		[UID_2] = 
		{
			[ProductID1] = 
			{...
			};
			[ProductID2] = 
			{...
			};
			...
		};
	}
```


### 示例 7

```lua
{
		[ProductID1] = -- 商品ID
		{
				BuyProductLimitCount = 3,  -- 剩余的限购数量
		}, 
		[ProductID2] = -- 商品ID
		{
				BuyProductLimitCount = 3,  -- 剩余的限购数量
		}, 
		...
	}
```


### 示例 8

```lua
-- 显示并获取界面对象实例
UGCCommoditySystem.ShowRechargeEntryUI():Then(
	function (Result)
		local UI = Result:Get()
		UI:SetVisibility
	end
)
```


### 示例 9

```lua
-- 在某个UI按钮点击后，调用购买函数
	function ShopUI:BuyItem()
			local Result = UGCCommoditySystem.BuyUGCCommodity2(
					9000001,
					UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/S_Icon.S_Icon'),
					"草药用于治疗，是非常好用的道具",
					1
			)
	end
```


### 示例 10

```lua
-- 购买商品后的结果，通过UGCCommoditySystem.BuyUGCCommodityResultDelegate返回，可以在GameState中增加和移除委托监听
	function UGCGameState:ReceiveBeginPlay()
			UGCGameState.SuperClass.ReceiveBeginPlay(self)
			UGCCommoditySystem.BuyUGCCommodityResultDelegate:Add(self.OnBuyUGCCommodityResult, self)
	end

	function UGCGameState:ReceiveEndPlay()
			UGCGameState.SuperClass.ReceiveEndPlay(self)
			UGCCommoditySystem.BuyUGCCommodityResultDelegate:Remove(self.OnBuyUGCCommodityResult, self)
	end

	--可以通过参数得知玩家物品的变化已经是否购买成功
	function UGCGameState:OnBuyUGCCommodityResult(bSucceeded, PlayerKey, CommodityID, Count, UID, ProductID)
			local Result = string.format(
					"(UGCGameState:OnBuyUGCCommodityResult bSucceeded=%s PlayerKey=%s CommodityID=%s Count=%s)", 
					tostring(bSucceeded), 
					tostring(PlayerKey), 
					tostring(CommodityID), 
					tostring(Count),
					tostring(ProductID)
			)
			ugcprint(Result)

			local cond = UGCGameSystem.IsServer()
			if cond then
					local Result = UGCCommoditySystem.GetAllPlayerUGCCommodityList()
					local PlayerController = UGCGameSystem.GetPlayerControllerByPlayerKey(PlayerKey)
					ugcprint(ToTreeString(Result[PlayerController:GetInt64UID()]))
			else
					local Result = UGCCommoditySystem.GetUGCCommodityList()
					ugcprint(ToTreeString(Result))
			end
	end
```


### 示例 11

```lua
-- 在某个UI按钮点击后，调用使用函数
	function ShopUI:OnClickItem()
			local Result = UGCCommoditySystem.UseUGCCommodity(
					1003,
					PlayerController,
					UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/S_Icon.S_Icon'),
					"草药用于治疗，是非常好用的道具",
					1,
					true
			)
	end
```


### 示例 12

```lua
--使用物品后的结果，通过UGCCommoditySystem.OnUseUGCCommodityResult返回，可以在GameState中增加和移除委托监听
	function UGCGameState:ReceiveBeginPlay()
			UGCGameState.SuperClass.ReceiveBeginPlay(self)
			UGCCommoditySystem.UseUGCCommodityResultDelegate:Add(self.OnUseUGCCommodityResult, self)
	end

	function UGCGameState:ReceiveEndPlay()
			UGCGameState.SuperClass.ReceiveEndPlay(self)
			UGCCommoditySystem.UseUGCCommodityResultDelegate:Remove(self.OnUseUGCCommodityResult, self)
	end

	-- 可以通过参数得知玩家物品的变化已经是否使用成功
	function UGCGameState:OnUseUGCCommodityResult(bSucceeded, PlayerKey, CommodityID, Count, UID)
			ugcprint("UGCGameState:OnUseUGCCommodityResult")

			local Result = string.format(
					"(UGCGameState:OnUseUGCCommodityResult bSucceeded=%s PlayerKey=%s CommodityID=%s Count=%s)", 
					tostring(bSucceeded), 
					tostring(PlayerKey), 
					tostring(CommodityID), 
					tostring(Count),
					tostring(UID)
			)
			ugcprint(Result)

			local cond = UGCGameSystem.IsServer()
			if cond then
					local Result = UGCCommoditySystem.GetAllPlayerUGCCommodityList()
					local PlayerController = UGCGameSystem.GetPlayerControllerByPlayerKey(PlayerKey)
					ugcprint(ToTreeString(Result[PlayerController:GetInt64UID()]))
			else
					local Result = UGCCommoditySystem.GetUGCCommodityList()
					ugcprint(ToTreeString(Result))
			end
	end
```


## 商城模板

> 来源: 商业化及功能模板 (id:20026)


### 示例 1

```lua
local PlayerController = STExtraGameplayStatics.GetFirstPlayerController(self);
local ShopMainUIClass = UE.LoadClass(UGCGameSystem.GetUGCResourcesFullPath('ExtendResource/ShopV2/OfficialPackage/Asset/ShopV2/Blueprint/ShopV2_OpenShopButton_UIBP.ShopV2_OpenShopButton_UIBP_C'));
local ShopMainUI = UserWidget.NewWidgetObjectBP(PlayerController, ShopMainUIClass);
ShopMainUI:AddToViewport(1000);
```


### 示例 2

```lua
UGCGameSystem.UGCRequire("ExtendResource.ShopV2.OfficialPackage.Script.ShopV2.ShopV2Manager")
```


### 示例 3

```lua
-- 生效范围：客户端
-- @param TabID number 页签ID
function ShopV2Manager:OpenMainUI(TabID)
end
```


### 示例 4

```lua
-- 生效范围：客户端
function ShopV2Manager:CloseMainUI()
end
```


### 示例 5

```lua
-- 生效范围：客户端/服务端
function ShopV2Manager:GetAllProductConfigData():LuaTable<ProductID, ProductData>
end
```


### 示例 6

```lua
-- 生效范围：客户端/服务端
-- @param ProductID number 商品ID
function ShopV2Manager:GetProductConfigData(ProductID):LuaTable<ProductData>
end
```


### 示例 7

```lua
-- 生效范围：客户端/服务端
-- @param ItemID number 物品ID
function ShopV2Manager:GetItemConfigData(ItemID):ObjectData
end
```


### 示例 8

```lua
-- 生效范围：客户端/服务端
function ShopV2Manager:GetAllItemConfigDatas():LuaTable<ItemID, ObjectData>
end
```


### 示例 9

```lua
-- 生效范围：客户端/服务端
-- @param ProductID number 商品ID
-- @param PlayerController UGCPlayerController 玩家控制器
function ShopV2Manager:GetLimitPurchasedTimes(ProductID, PlayerController):number
end
```


### 示例 10

```lua
-- 生效范围：客户端/服务端
-- @param PlayerController UGCPlayerController 玩家控制器
function ShopV2Manager:GetAllLimitPurchasedProducts(PlayerController):LuaTable<ProductID, Count>
end
```


### 示例 11

```lua
-- 生效范围：客户端/服务端
-- @param ProductID number 商品ID
---@param PlayerController UGCPlayerController
function ShopV2Manager:GetPurchasedTimes(ProductID, PlayerController):number
end
```


### 示例 12

```lua
-- 生效范围：客户端/服务端
---@param PlayerController UGCPlayerController
function ShopV2Manager:GetAllPurchasedProducts(PlayerController):LuaTable<ProductID, Count>
end
```


## 抽奖模板

> 来源: 商业化及功能模板 (id:20027)


### 示例 1

```lua
---@param bVisible bool 是否打开界面
---生效范围：客户端
function LotteryManager:SetLotteryPanelVisible(bVisible)
end
```


### 示例 2

```lua
---生效范围：客户端&&服务端
---@param ItemID number 物品ID
function LotteryManager:GetItemConfigData(ItemID):ItemConfigData
end
```


### 示例 3

```lua
---生效范围：客户端&&服务端
---@param ProductID number 商品ID
function LotteryManager:GetProductConfigData(ProductID):ProductConfigData
end
```


### 示例 4

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
function LotteryManager:GetLotteryConfigData(LotteryID):LotteryConfigData
end
```


### 示例 5

```lua
---生效范围：客户端
---@param LotteryID number 奖池ID
function LotteryManager:SwitchLotteryTab(LotteryID)
end
```


### 示例 6

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:DrawLotteryOnce(LotteryID, PlayerController)
end
```


### 示例 7

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:DrawLotteryTenTimes(LotteryID, PlayerController)
end
```


### 示例 8

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:GetPlayerTodayDraws(LotteryID, PlayerController):number
end
```


### 示例 9

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:GetPlayerTotalDraws(LotteryID, PlayerController):number
end
```


### 示例 10

```lua
---生效范围：客户端
---@param ProductID number 商品ID
---@param ExchangeNum number 兑换数量
function LotteryManager:RedeemProduct(ProductID, ExchangeNum)
end
```


### 示例 11

```lua
---生效范围：客户端&&服务端
---@param ItemID number 道具ID
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:GetPlayerOwnedItemNum(ItemID, PlayerController):number
end
```


### 示例 12

```lua
---生效范围：客户端&&服务端
---@param ProductID number 商品ID
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:GetProductExchangeNum(ProductID, PlayerController):number
end
```


### 示例 13

```lua
---生效范围：客户端&&服务端
---@param ProductID number 商品ID
function LotteryManager:IsProductExpired(ProductID):bool
end
```


### 示例 14

```lua
---生效范围：客户端&&服务端
---@param ProductID number 商品ID
function LotteryManager:IsProductSoldOut(ProductID):bool
end
```


### 示例 15

```lua
---生效范围：客户端&&服务端
---@param GroupID number 掉落组ID
function LotteryManager:GetLotteryIDByDrawGroup(DrawGroup):LuaTable<LotteryID>
end
```


### 示例 16

```lua
---生效范围：客户端&&服务端
---@param ProductID number 商品ID
function LotteryManager:GetItemIDByProduct(ProductID):number
end
```


### 示例 17

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
function LotteryManager:GetGiftPackageConfigData(LotteryID):LuaTable<ProgressLevel, <ItemID, num>>
end
```


### 示例 18

```lua
---生效范围：客户端&&服务端
---@param LotteryID number 奖池ID
---@param ProgressLevel number 抽奖进度
---@param PlayerController UUGCPlayerController 玩家控制器
function LotteryManager:AddGiftPackage(LotteryID, ProgressLevel, PlayerController)
end
```


### 示例 19

```lua
---生效范围：客户端&&服务端
-- @param ItemList 物品列表
LotteryManager.OnDrawLotteryOnceDelegate [MulticastDelegate] fun(ItemList:LuaTable<{ItemID:number, ItemNum:number}>)
```


### 示例 20

```lua
---生效范围：客户端&&服务端
-- @param ItemList 物品列表
LotteryManager.OnDrawLotteryTenTimesDelegate [MulticastDelegate] fun(ItemList:LuaTable<{ItemID:number, ItemNum:number}>)
```


### 示例 21

```lua
---生效范围：客户端&&服务端
-- @param Success 是否成功
LotteryManager.OnAddGiftPackageDelegate [MulticastDelegate] fun(Success:bool)
```


## 礼包模板

> 来源: 商业化及功能模板 (id:20049)


### 示例 1

```lua
local GiftPackUIClass = UE.LoadClass(UGCGameSystem.GetUGCResourcesFullPath("ExtendResource/GiftPack/OfficialPackage/Asset/GiftPack/Blueprint/WBP_GiftPackBtn.WBP_GiftPackBtn_C"));
local PlayerController = STExtraGameplayStatics.GetFirstPlayerController(self);
local GiftPackUI = UserWidget.NewWidgetObjectBP(PlayerController, GiftPackUIClass);
GiftPackUI:AddToViewport(12000);
```


### 示例 2

```lua
UGCGameSystem.UGCRequire("ExtendResource.GiftPack.OfficialPackage.Script.GiftPack.GiftPackManager")
```


### 示例 3

```lua
-- 生效范围：客户端&&服务端
-- @param ItemID number 物品ID
function GiftPackManager:GetGiftPackageType(ItemID):GiftPackageType
end
```


### 示例 4

```lua
-- 生效范围：客户端&&服务端
-- @param GiftPackID number 礼包ID
-- @param UseNum number 打开的礼包数量
-- @param PlayerController UGCPlayerController 玩家控制器
function GiftPackManager:OpenNormalGiftPackage(GiftPackID, UseNum, PlayerController)
end
```


### 示例 5

```lua
-- 生效范围：客户端&&服务端
-- @param GiftPackID number 礼包ID
-- @param UseNum number 打开的礼包数量
-- @param ItemList LuaTable<ItemID，Num> 选择的物品列表
-- @param PlayerController UGCPlayerController 玩家控制器
function GiftPackManager:OpenOptionalGiftPackage(GiftPackID, UseNum, ItemList, PlayerController)
end
```


### 示例 6

```lua
-- 生效范围：客户端&&服务端
-- @param GiftPackID number 礼包ID
function GiftPackManager:GetPackageDropItems(GiftPackID):LuaTable<ItemID，Num>
end
```


### 示例 7

```lua
-- 生效范围：服务端
-- @param GiftPackID number 礼包ID
---@param Num number 发放数量
---@param PlayerController UUGCPlayerController 玩家控制器
function GiftPackManager:AddGiftPackage(GiftPackID, Num, PlayerController):bool
end
```


### 示例 8

```lua
---生效范围：客户端&&服务端
-- @param ItemList 获得物品列表
GiftPackManager.OnOpenGiftPackageDelegate [MulticastDelegate] fun(ItemList:LuaTable<{ItemID:number, ItemNum:number}>)
```


## 玩家数据存档

> 来源: 进阶内容 (id:20052)


### 示例 1

```lua
SaveData = {}

-- 基础类型元素
SaveData[1] = "Lua"
SaveData["wow"] = "abc"

-- table类型元素
local SubTableData = {}
table.insert(SubTableData,"equip")
table.insert(SubTableData,"avatar")
SaveData["attributeList"] = SubTableData
```


### 示例 2

```lua
local equipSequence = {}

local playerPawn = self:GetPlayerCharacterSafety()
local allItemData = UGCBackPackSystem.GetAllItemData(playerPawn)
for key, itemData in pairs(allItemData) do
	table.insert(equipSequence, itemData.ItemID)
end

local playerUID = UGCPawnAttrSystem.GetPlayerUID(playerPawn)
UGCPlayerStateSystem.SavePlayerArchiveData(tonumber(playerUID), equipSequence)
```


### 示例 3

```lua
local playerPawn = self:GetPlayerCharacterSafety()
	
local playerUID = UGCPawnAttrSystem.GetPlayerUID(playerPawn)
local readArchive = UGCPlayerStateSystem.GetPlayerArchiveData(tonumber(playerUID))
for i = 1, #readArchive do
	UGCBackPackSystem.AddItem(playerPawn, readArchive[i], 1)
end
```


## 通用掉落表

> 来源: 通用功能 (id:20058)


### 示例 1

```lua
local CurrentPlayerPawn = UGCGameSystem.GetPlayerPawnByPlayerController(self)
local CurrentLocation = CurrentPlayerPawn:K2_GetActorLocation()
local DropLocation = UGCMathUtility.AddVector(CurrentLocation, Vector.New(200,0,0))
UGCItemSystemV2.StartCustomizeDrop(DropLocation, DropID, -1, EUGCGenerateItemEntityType.GenerateItemEntity_WrapperActor, CurrentPlayerPawn)
```


## 签到模板

> 来源: 商业化及功能模板 (id:20060)


### 示例 1

```lua
local PlayerController = STExtraGameplayStatics.GetFirstPlayerController(self);
local SignInMainUIClass = UE.LoadClass(UGCGameSystem.GetUGCResourcesFullPath('ExtendResource/SignInEvent/OfficialPackage/Asset/SignInEvent/Blueprint/TestButton.TestButton_C'));
local SignInMainUI = UserWidget.NewWidgetObjectBP(PlayerController, SignInMainUIClass);
SignInMainUI:AddToViewport(1000);
```


### 示例 2

```lua
UGCGameSystem.UGCRequire("ExtendResource.SignInEvent.OfficialPackage.Script.SignInEvent.SignInEventManager")
```


### 示例 3

```lua
-- 生效范围：客户端
function SignInEventManager:OpenMainUI()
end
```


### 示例 4

```lua
-- 生效范围：客户端
function SignInEventManager:CloseMainUI()
end
```


### 示例 5

```lua
-- 生效范围：客户端&&服务端
-- @param EventID number 签到活动ID
function SignInEventManager:GetEventConfigData(EventID):EventConfigData
end
```


### 示例 6

```lua
-- 生效范围：客户端&&服务端
-- @param EventID number 签到活动ID
-- @param PlayerController UGCPlayerController 玩家控制器
function SignInEventManager:GetEventDayNum(EventID, PlayerController):number
end
```


## GamePart模块化

> 来源: 通用功能 (id:20090)


### 示例 1

```lua
-- 获取GamePartGlobalActor的实例对象
local GamePartActorInstance = UGCGamePartSystem.GetGamePartGlobalActor("CommodityOperationManager");

-- 调用模块GamePart接口获取商品配置信息
local CommodityConfigData = GamePartActorInstance:GetAllProductData();
log_tree(CommodityConfigData)
```


### 示例 2

```lua
function UGCGameState:ReceiveBeginPlay()
		local GamePartActorInstance = UGCGamePartSystem.GetGamePartGlobalActor("VirtualItemManager");
  	if GamePartActorInstance then
    		GamePartActorInstance.AddItemResultDelegate:Add(self.OnAddVirtualItemResult, self)
  	else
    		UGCGenericMessageSystem.ListenGlobalMessage(self, UGCGenericMessageSystem.Messages.UGC.GamePart.GamePartLoaded, self, self.OnGamePartLoaded)
  	end
end

function UGCGameState:OnGamePartLoaded(GamePartName)
    if GamePartName == 'VirtualItemManager' then
        local GamePartActorInstance = UGCGamePartSystem.GetGamePartGlobalActor("VirtualItemManager");
        GamePartActorInstance.AddItemResultDelegate:Add(self.OnAddVirtualItemResult, self);
    end
end

function UGCGameState:OnAddVirtualItemResult()
	-- 当获得虚拟物品时执行的逻辑
end
```


## 技能Task查询手册

> 来源: 进阶内容/GamePlay系统/技能系统/技能编辑器2.0 (id:20094)


### 示例 1

```lua
local TestCustomLua = {}

-- 该Task初始化时调用,即Task获取到SkillOwner,即该Task所属Section触发时调用
function TestCustomLua:InitTask_BP()
end

-- 该Task是否支持RPC
function TestCustomLua:TaskSupportRPC_BP()
  return false
end

-- 当该Task被激活时调用
function TestCustomLua:OnActivate_BP()
end

-- 当该Task被反激活时调用
function TestCustomLua:OnDeactivate_BP()
end

-- 当该Task被Trigger时调用，Trigger的间隔由Task配置的间隔决定
function TestCustomLua:OnTrigger_BP(Delta)
end
```


## 通用属性系统

> 来源: 进阶内容/GamePlay系统/属性与伤害 (id:20098)


### 示例 1

```lua
-- 获得的耐力值*3
function UGCAttributeGroup_Character:GetEndurance_Override(OriginalValue, AttributeOwnerActor)  
    return OriginalValue * 3  
end  

return UGCAttributeGroup_Character
```


### 示例 2

```lua
UGCGameSystem.UGCRequire('Script.GameAttribute.game_attribute_type')

local Hp = UGCAttributeSystem.GetGameAttributeValue(CauserActor, UGCNativeGameAttributeType.Character_Health)
-- 获取血量后的其他逻辑处理
```


## 全局伤害公式

> 来源: 进阶内容/GamePlay系统/属性与伤害 (id:20099)


### 示例 1

```lua
local RestrictedDamageType = UGCAttributeSystem.GetDamageTypeFromContext(Context)
local DamageTypeTags = UGCAttributeSystem.GetDamageTagsFromContext(Context)
 
for _, Tag in pairs(DamageTypeTags) do
	if Tag then
		print("[UGCGlobalDamageCalculation] Context Tag: --->"..Tag)
	end
end
```


### 示例 2

```lua
local CritTag = UGCGameplayTagSystem.RequestGameplayTag("UGC.Damage.Result.Critical")
if CritTag then
	ExtraResult.ResultTags:Add(CritTag)
end
```


### 示例 3

```lua
-- 定义回调函数
local Callback = function(PlayerPawn, DamageCauserActor, EventInstigator, Damage, DamageContext)
	if DamageContext then
		print("DamageContext exists")
		if DamageContext and DamageContext.ResultTags then
			print("ResultTags exists:" .. tostring(DamageContext.ResultTags))
    else
			print("ResultTags is nil")
		end
	end

	-- 反监听PostTakeDamage事件
	UGCGenericMessageSystem.UnListenObjectMessage(PlayerPawn, UGCGenericMessageSystem.Messages.UGC.PlayerPawn.PostTakeDamage)
end

-- 监听PostTakeDamage事件
UGCGenericMessageSystem.ListenObjectMessage(PlayerPawn, UGCGenericMessageSystem.Messages.UGC.PlayerPawn.PostTakeDamage, nil, Callback)
```


## 物品编辑器

> 来源: 进阶内容/GamePlay系统/物资系统/物资编辑器2.0 (id:20101)


### 示例 1

```lua
function LightSpeedChicken:OnUseV2()
    LightSpeedChiken.SuperClass.OnUseV2(self);

    local SkillPath = UGCMapInfoLib.GetRootLongPackagePath() .. "Asset/Blueprint/Prefabs/Buffs/LightSpeedChicken.LightSpeedChicken_C"
    local SkillClass = UGCObjectUtility.LoadClass(SkillPath)

    local OwnBackpackComponent = UGCItemSystemV2.GetOwnBackpackComponent(self) --获取背包组件
    local PlayerController = OwnBackpackComponent:GetOwner() --获取玩家自身PlayerController
    local PlayerPawn = UGCGameSystem.GetPlayerPawnByPlayerController(PlayerController)

    if PlayerPawn then
        UGCPersistEffectSystem.AddBuffByClass(PlayerPawn, SkillClass)
    end
end
```


## 背包系统

> 来源: 进阶内容/GamePlay系统/物资系统/物资编辑器2.0 (id:20104)


### 示例 1

```lua
-- Data形参结构
Data = {
	idx, -- 当前位置索引 number
	InstanceID, -- 物品实例ID number
	DefineID, -- 物品数据 FItemDefineID
}

---@param Data1 table 前一个物品数据
---@param Data2 table 后一个物品数据
---@return bool 是否保持位置不变
function BP_BackpackUIComponentV2_Custom.CompareQuantity(Data1,Data2)
	-- TODO 实现按数量排序的逻辑
end
```


### 示例 2

```lua
function BackpackUIComponent.CompareQuality(Data1,Data2)
    local ItemDefinID1 = Data1.DefineID;
    local ItemDefinID2 = Data2.DefineID;
    if ItemDefinID1 == nil or ItemDefinID2 == nil then
        print("BackpackUIComponent:CompareQuality error InstanceID1:"..tostring(InstanceID1)..",InstanceID2:"..tostring(InstanceID2))
        return
    end
    local quality1 = UGCItemSystemV2.GetItemQualityV2(ItemDefinID1.TypeSpecificID)
    local quality2 = UGCItemSystemV2.GetItemQualityV2(ItemDefinID2.TypeSpecificID)
    --品质排序
    if quality1 ~= quality2 then
        return quality1 > quality2 -- 物品1品质大于物品2品质，则保持位置不变
    end
    return Data1[1] < Data2[1] -- 品质相同，则物品1原始位置在前，则保持位置不变
end
```


### 示例 3

```lua
---@field 3DUI_Icon UImage
local 3DUI_Text = {}

-- 内部约定好的函数名称InitData
-- @param DataTable table @物品数据 {ItemDefineID:物品FItemDefineID, DataType:EItemDataTypeStrs背包物品类型, ItemID:物品ID}
function 3DUI_Text:InitData(DataTable)
    -- 异步加载物品图标纹理并设置到Image控件
    local weakSelf = WeakObjectPtr(self)
    UGCObjectUtility.AsyncLoadObjectBySoftPath(UGCItemSystemV2.GetItemIconTextureV2(DefineID.TypeSpecificID), function(LoadedTexture)
        if weakSelf:IsValid() then
            weakSelf:Get().3DUI_Icon:SetBrushFromTexture(LoadedTexture, true)
        end
    end)
end
```


### 示例 4

```lua
---点击上锁格子的响应函数
---生效范围：客户端
---@param DataType number @类型 [0:背包数据, 1:仓库数据]
function BP_BackpackUIComponentV2_Custom:ClickLockBackpackItem(DataType)
    local PlayerController = self:GetOwner()
    local itemCount = UGCBackpackSystemV2.GetItemCountV2(PlayerController, 8310001)
    -- 判断条件，例如背包内有指定数量的道具才能解锁背包格子
    if itemCount >= 10 then
        -- 满足则解锁1个背包格子
        UGCBackpackSystemV2.AddCellCapacity(PlayerController, 1)
    end
end
```


### 示例 5

```lua
--在内置规则上叠加条件
function BP_BackpackUIComponentV2_Custom:CheckCustomItem(DefineID, DataType)
  	-- 8310000为物编创建的能量饮料物品id
    if DefineID.TypeSpecificID == 8310000 then
				-- 获取内置条件结果
        local bCheckUse = self:CheckUseItem(DefineID, DataType)
        return bCheckUse    
    else
				return false
    end
end
```


### 示例 6

```lua
function BP_Func_Button:OnClickOpenWareHouse()
    UGCBackpackSystemV2.OpenBackpackPanel(5)
end
```


## 状态互斥

> 来源: 进阶内容/GamePlay系统/技能系统 (id:20106)


### 示例 1

```lua
-- 判断角色是否允许跳跃
local JumpTag = UGCGameplayTagSystem.RequestGameplayTag("PawnState.Action.Jump")
local CheckResult = UGCPersistEffectSystem.AllowDynamicState(self:GetOwnerActor(), JumpTag)

-- 支持Tag以字符型传参
local CheckResult = UGCPersistEffectSystem.AllowDynamicState(self:GetOwnerActor(), "PawnState.Action.Jump")
```


### 示例 2

```lua
function UGCPlayerPawn:ReceiveBeginPlay()
	UGCPlayerPawn.SuperClass.ReceiveBeginPlay(self)
	
	-- 绑定委托
	local PEComponent = UGCPersistEffectSystem.GetPersistBaseComponentByContent(self)
	PEComponent.DynamicStateInterruptedHandle:Add(self.OnFlyInterrupt, self);
end

-- 委托回调函数
function UGCPlayerPawn:OnFlyInterrupt(InterruptTag)
	if UGCGameplayTagSystem.EqualsTag(InterruptTag, "PawnState.Movement.Fly") then
		-- 实现被打断后的处理逻辑
	end
end
```


### 示例 3

```lua
-- 打断交互状态
UGCPersistEffectSystem.InterruptDynamicState(self, "PawnState.Interacting")

-- 禁用持有武器
UGCPersistEffectSystem.SetDynamicStateDisabled(self, "PawnState.Action.HoldWeapon")
```


## 通用GM面板

> 来源: 绿洲编辑器基础内容/调试游戏 (id:20109)


### 示例 1

```lua
local GM = {}

function GM:Register(DebugUI)
    local UGCGMUI = require("client.ingame.ugc.ugc_gmui")

    local CurFuncList = {}
    -- TODO 此处扩展GM配置
    return CurFuncList
end

return GM
```


### 示例 2

```lua
GM:{函数前缀}{函数名}(参数1, 参数2)
```


### 示例 3

```lua
---@param Param string 参数字符串
---@param PC UGCPlayerController 玩家控制器（仅DS有值，客户端值为nil）
function GM:CS_Switch(Param, PC)
    -- print("GM:Function called CS_Switch"..str)
end
```


### 示例 4

```lua
---@param Param string 参数字符串
function GM:C_GetItemNum(Param)
    local Num1, Num2 = string.match(Param, "(%-?%d+%.?%d*) (%-?%d+%.?%d*)")
		print(string.format("C_GetItemNum Num1:%d Num2:%d", Num1, Num2))
end
```


### 示例 5

```lua
---@param Param string 参数字符串
---@param PC UGCPlayerController 玩家控制器
function GM:S_AddItems(Param, PC)
    local ItemID, Num = string.match(Param, "(%-?%d+%.?%d*) (%-?%d+%.?%d*)")
		print(string.format("S_AddItems Item:%d Num:%d", ItemID, Num))
		
		local PlayerPawn = UGCGameSystem.GetPlayerPawnByPlayerController(PC)
		UGCBackpackSystemV2.AddItemV2(PlayerPawn, ItemID, Num)
end
```


### 示例 6

```lua
-- 注册GM指令：将各调试控件与对应的GM函数进行绑定
function GM:Register(DebugUI)
    local UGCGMUI = require("client.ingame.ugc.ugc_gmui")
    local CurFuncList = {}

    CurFuncList["调试"] = {
        ["SubTab1"] = {
            {UGCGMUI.ItemTypeEnum.Button, {{"双端打印"}, {"点击后执行CS_Func"}}, "CS_Func"},
            {UGCGMUI.ItemTypeEnum.TextInput, {{"修改玩家当前生命", "输入格式：health healthMax"}, {"修改玩家生命"}}, "S_ChangeHealth"},
            {UGCGMUI.ItemTypeEnum.Slider, {{"Slider", {0, 100, 40}}, {"用于调节参数值"}}, "CS_SliderbarFunc"},
            {UGCGMUI.ItemTypeEnum.SwitchButton, {{"开关", function() return "close" end}, {"切换状态"}}, "CS_Switch"},
        },
    }
		
    return CurFuncList
end
```


## 虚拟物品

> 来源: 商业化及功能模板 (id:20139)


### 示例 1

```lua
-- DS发放虚拟物品
local GamePartActor = UGCGamePartSystem.GetGamePartGlobalActor("VirtualItemManager");
if GamePartActor then
	GamePartActor:AddVirtualItem(self, 1001, 3);
end

-- 客户端查询拥有的虚拟物品数据
local GamePartActor = UGCGamePartSystem.GetGamePartGlobalActor("VirtualItemManager");
if GamePartActor then
	local OwnedItems = GamePartActor:GetOwnedVirtualItems()
  log_tree(OwnedItems)
end
```


## 怪物快速配置指南

> 来源: 进阶内容/GamePlay系统/怪物系统 (id:20144)


### 示例 1

```lua
-- 设置实体类型标签
local EntityTypeTag = UGCGameplayTagSystem.RequestGameplayTag("LogicPart.Filter.EntityType.Melee")
MonsterObject:SetEntityTypeTag(EntityTypeTag)

-- 查询实体类型标签
local TagInfo = MonsterObject:GetEntityTypeTag()
ugcprint(string.format("Monster Entity Type: %s", TagInfo.TagName))
```


## 怪物血条

> 来源: 进阶内容/GamePlay系统/怪物系统 (id:20145)


### 示例 1

```lua
---@class WBP_Custom1_C:UGCGenericCharacterPositionWidget
local WBP_Custom1 = { bInitDoOnce = false } 

-- 角色的名字发生变化
---@param Name FName 变化后的名字
function WBP_Custom1:BP_CharacterNameChange(Name)
    ugcprint(self.OwnerCharacter)
end

-- 角色的血量发生变化
---@param InHPCurrent float 变化后的血量
---@param InHPMax float 变化后的血量上限
function WBP_Custom1:BP_CharacterHPChange(InHPCurrent,InHPMax)
    
end

-- 初始化参数结束
function WBP_Custom1:Event_InitParamEnd()
    
end

return WBP_Custom1
```


## 物品掉落与保留

> 来源: 进阶内容/GamePlay系统/角色系统 (id:20148)


### 示例 1

```lua
function UGCPlayerPawn:IsSkipSpawnDeadTombBox(EventInstigater)
    		return true
	end
```


## 通用运动组件

> 来源: 通用功能 (id:20150)


### 示例 1

```lua
function UGCPlayerController:ServerRPC_TestUGCMotion(TestType, MotionIndex)
    --调用测试函数ServerRPC_TestUGCMotion，传入运动器当前状态TestType和运动器id MotionIndex
    
    local UGCMotionTestActorClassPath = UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/Cube_Blueprint.Cube_Blueprint_C')
		--获取添加了运动器组件的Cube_Blueprint组件的完整路径，赋值给UGCMotionTestActorClassPath
		
    local UGCMotionTestActorClass = UGCObjectUtility.LoadClass(UGCMotionTestActorClassPath)
    --创建一个运动对象的类 UGCMotionTestClass，获取赋值给UGCMotionTestActorClassPath的完整路径

		---@type Cube_Blueprint_C[]
    local UGCMotionTestActorList = UGCActorComponentUtility.GetAllActorsOfClass(self, UGCMotionTestActorClass)
    --获取场景中所有根据UGCMotionTestClass类创建出来的对象
    
    for _, MotionActor in pairs(UGCMotionTestActorList) do
        --遍历场景中所有的运动对象
        local UGCMotionComp = MotionActor.UGCMotion
        --获取每个运动对象的运动组件实例
				UGCMotionComp:StartMotion(MotionIndex)
				--调用运动组件实例的方法
    end
end
```


### 示例 2

```lua
function UGCPlayerController:ServerRPC_TestUGCMotion(TestType, MotionIndex)
    --调用测试函数ServerRPC_TestUGCMotion，传入运动器当前状态TestType和运动器id MotionIndex
    
    local UGCMotionTestActorClassPath = UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/Cube_Blueprint.Cube_Blueprint_C')
		--获取添加了运动器组件的Cube_Blueprint组件的完整路径，赋值给UGCMotionTestActorClassPath
		
    local UGCMotionTestActorClass = UGCObjectUtility.LoadClass(UGCMotionTestActorClassPath)
    --创建一个运动对象的类 UGCMotionTestClass，获取赋值给UGCMotionTestActorClassPath的完整路径

		---@type Cube_Blueprint_C[]
    local UGCMotionTestActorList = UGCActorComponentUtility.GetAllActorsOfClass(self, UGCMotionTestActorClass)
    --获取场景中所有根据UGCMotionTestClass类创建出来的对象
    
    for _, MotionActor in pairs(UGCMotionTestActorList) do
        --遍历场景中所有的运动对象
        local UGCMotionComp = MotionActor.UGCMotion
        --获取每个运动对象的运动组件实例
				UGCMotionComp:PauseMotion(MotionIndex)
				--调用运动组件实例的方法
    end
end
```


### 示例 3

```lua
function UGCPlayerController:ServerRPC_TestUGCMotion(TestType, MotionIndex)
    --调用测试函数ServerRPC_TestUGCMotion，传入运动器当前状态TestType和运动器id MotionIndex
    
    local UGCMotionTestActorClassPath = UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/Cube_Blueprint.Cube_Blueprint_C')
		--获取添加了运动器组件的Cube_Blueprint组件的完整路径，赋值给UGCMotionTestActorClassPath
		
    local UGCMotionTestActorClass = UGCObjectUtility.LoadClass(UGCMotionTestActorClassPath)
    --创建一个运动对象的类 UGCMotionTestClass，获取赋值给UGCMotionTestActorClassPath的完整路径

		---@type Cube_Blueprint_C[]
    local UGCMotionTestActorList = UGCActorComponentUtility.GetAllActorsOfClass(self, UGCMotionTestActorClass)
    --获取场景中所有根据UGCMotionTestClass类创建出来的对象
    
    for _, MotionActor in pairs(UGCMotionTestActorList) do
        --遍历场景中所有的运动对象
        local UGCMotionComp = MotionActor.UGCMotion
        --获取每个运动对象的运动组件实例
				UGCMotionComp:ResetMotion(MotionIndex)
				--调用运动组件实例的方法
    end
end
```


## 属性修改器

> 来源: 进阶内容/GamePlay系统/属性与伤害 (id:20153)


### 示例 1

```lua
f(X) = 值覆盖修改器 ? 值覆盖修改器 : (X + 加法修改器1 + 加法修改器2...) * (乘法修改器1 + 乘法修改器2...) * 独立乘法修改器1 * 独立乘法修改器2... + 独立加法修改器1 + 独立加法修改器2...
```


## 怪物刷新器

> 来源: 进阶内容/GamePlay系统/怪物系统 (id:20156)


### 示例 1

```lua
-- 从怪物路径配置表中获取蓝图类
function MonsterSpawner:GetMonsterClass(MonsterID, MonsterType)
    print("MonsterSpawner:GetMonsterClass")
    local FinallyMonsterID = -1
    if MonsterType == "LittleMonster" then
        FinallyMonsterID = tonumber(tostring(10)..MonsterID)
    end

    if MonsterType == "EliteMonster" then
        FinallyMonsterID = tonumber(tostring(20)..MonsterID)
    end

    if MonsterType == "Boss" then
        FinallyMonsterID = tonumber(tostring(30)..MonsterID)
    end

    local MonsterPathTable = UGCGameSystem.GetTableData("Data/Table/MonsterPathTable")
    if not MonsterPathTable then
        print("MonsterSpawner:GetMonsterClass1")
        return nil
    end

    for _, value in pairs(MonsterPathTable) do
        print("MonsterSpawner:GetMonsterClass"..value.MonsterID)
        if value.MonsterID == FinallyMonsterID then
						return UGCObjectUtility.LoadObjectBySoftPath(value.MonsterPath)
				end
		end
		
    return nil
end

-- 从怪物详情配置表中查找怪物属性配置
function MonsterSpawner:GetMonsterAttributes(MonsterID, MonsterType)
    print("MonsterSpawner:GetMonsterAttributes")
    local FinallyMonsterID = -1
    if MonsterType == "LittleMonster" then
        FinallyMonsterID = tonumber(tostring(10)..MonsterID)
    end

    if MonsterType == "EliteMonster" then
        FinallyMonsterID = tonumber(tostring(20)..MonsterID)
    end

    if MonsterType == "Boss" then
        FinallyMonsterID = tonumber(tostring(30)..MonsterID)
    end

    local MonsterAttributeTable = UGCGameSystem.GetTableData("Data/Table/MonsterAttributeTable")
    if not MonsterAttributeTable then
        return nil
    end

    print("MonsterSpawner:GetMonsterAttributes1")
    for _, value in pairs(MonsterAttributeTable) do
        print("MonsterSpawner:GetMonsterAttributes"..value.MonsterID..FinallyMonsterID)
        if value.MonsterID == FinallyMonsterID then
        		print("MonsterSpawner:GetMonsterAttributes2")
        		return value.Attributes
    		end
		end

    return nil
end


function MonsterSpawner:CustomSpawnMob(InCustomParam)
    print("MonsterSpawner:CustomSpawnMob")
    -- 查找怪物蓝图路径
    local MonsterClass = self:GetMonsterClass(InCustomParam.MonsterID, InCustomParam.MonsterType)
    if not MonsterClass then
        return nil
    end

    local SpawnedMonster = self:SpawnMob(MonsterClass)

    -- 查找怪物属性配置
    local MonsterAttributes = self:GetMonsterAttributes(InCustomParam.MonsterID, InCustomParam.MonsterType)
    if not MonsterAttributes then
        return SpawnedMonster
    end

    -- 初始化怪物属性
    self:InitMonsterAttr(SpawnedMonster, MonsterAttributes)

		--对于自定义方法进行刷怪，在使用路点移动时，需要手动调用
		--黑板设置使用路点值
  	UGCGenericCharacterSystem.GetBlackboard(SpawnedMonster):SetValueAsBool("bUsePathPoint",true)
		
		--获取路点Location信息
    self.WaypointsLocation={}
    for index, pointActor in pairs(self.STSpawnerWaypoint.WayPointArr) do
        self.WaypointsLocation[index] = pointActor:K2_GetActorLocation()
    end
		
		--把路点信息传递给刷出的怪
    SpawnedMonster:GetLogicPartManagerInterface():GetFollowWayPointPartInterface():SetWaypoints(self.WaypointsLocation,true)
		
    return SpawnedMonster
end


-- 初始化怪物的指定属性值
function MonsterSpawner:InitMonsterAttr(Monster, Attributes)
    print("MonsterSpawner:InitMonsterAttr")
    if not Monster then
        return
    end

    for _, AttrItem in pairs(Attributes) do
        UGCAttributeSystem.SetGameAttributeValue(Monster, AttrItem.Attr.AttributeName, AttrItem.Value)
    end
end
```


## 通用抛体

> 来源: 通用功能 (id:20165)


### 示例 1

```lua
local ProjectileClass = UGCObjectUtility.LoadClass(UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/Prefabs/Projectiles/Proj_Poison.Proj_Poison_C'))
local PlayerPawn = self:GetPlayerCharacterSafety()
local SelfPosition = PlayerPawn:K2_GetActorLocation()
local PlayerDirection = PlayerPawn:GetActorForwardVector()

local LaunchPosition = Vector.New(SelfPosition.X, SelfPosition.Y + 300, SelfPosition.Z + 100)
UGCProjectileSystemV2.CreateProjectile(ProjectileClass, self, LaunchPosition, PlayerDirection, 2000, 1, 20, ERestrictedDamageType.SkillDamage)
```


## 关卡管理

> 来源: 玩法案例及模板/多人PVE模板 (id:20196)


### 示例 1

```lua
-- UGCLevelReward.lua

local UGCLevelReward = {}

function UGCLevelReward:LuaExecute()
	-- 获取当前关卡内的所有玩家
   	 local AllPlayer = UGCLevelFlowSystem.GetAllPlayerControllerInCurrentLevel()
    
	-- 每个玩家进行关底商店
	if AllPlayer and #AllPlayer > 0 then
		for k, v in pairs(AllPlayer) do
			v:CallShop()
		end
	else
		print("UGCLevelReward:LuaExecute AllPlayer is nil")
	end
	
	-- 关底奖励完成，进入结算
	self:OnFinish()
end

return UGCLevelReward
```


### 示例 2

```lua
-- UGCSettlement.lua

local UGCSettlement = {}

function UGCSettlement:LuaExecuteWithFinish(_, IsFinish)
	-- 获取当前关卡内的所有玩家
   	 local AllPlayer = UGCLevelFlowSystem.GetAllPlayerControllerInCurrentLevel()
    
	-- 每个玩家进行结算
	if AllPlayer and #AllPlayer > 0 then
		for k, v in pairs(AllPlayer) do
			v:CallSettle()
		end
	else
		print("UGCSettlement:LuaExecuteWithFinish AllPlayer is nil")
	end
	
	-- 关底结算完成，跳转到下个关卡
	self:OnFinish()
end

return UGCSettlement
```


### 示例 3

```lua
-- UGCResetData.lua

local UGCResetData = {}

function UGCResetData:LuaExecute()
	-- 获取当前关卡内的所有玩家
   	 local AllPlayer = UGCLevelFlowSystem.GetAllPlayerControllerInCurrentLevel()
    
	-- 每个玩家进行重置数据
	if AllPlayer and #AllPlayer > 0 then
		for k, v in pairs(AllPlayer) do
			v:CallReset()
		end
	else
		print("UGCSettlement:LuaExecuteWithFinish AllPlayer is nil")
	end
end

return UGCResetData
```


### 示例 4

```lua
-- UGCTimeOut.lua

local UGCTimeOut = {}

function UGCTimeOut:LuaExecute()
    -- 获取当前游戏内的所有玩家
    local AllPlayer = UGCLevelFlowSystem.GetAllPlayerControllerInCurrentLevel()

    if AllPlayer and #AllPlayer > 0 then
        for k, Player in pairs(AllPlayer) do
            -- 如果超时，逐个玩家调用失败结算，设置下面变量后会自动弹出超时失败结算界面
            if not Player.SettleParams.bIsSettled then
                Player.SettleParams.bIsSettled = true
                Player.SettleParams.bIsFinished = false
                DOREPONCE(Player, "SettleParams")
            end
        end
    else
        print("UGCTimeOut:LuaExecute AllPlayer is nil")
    end

end

return UGCTimeOut
```


### 示例 5

```lua
-- UGCSettlement.lua

local UGCSettlement = {}

function UGCSettlement:LuaExecuteWithFinish(_, IsFinish)

    -- 获取当前游戏内的所有玩家
    local AllPlayer = UGCLevelFlowSystem.GetAllPlayerControllerInCurrentLevel()
    if AllPlayer and #AllPlayer > 0 then
        for k, Player in pairs(AllPlayer) do
            
            -- 在玩家结算的时候更新游戏时间
            Player.PlayerState:UpdateGameTime()

            -- 通知玩家调出结算UI
            Player.SettleParams.bIsSettled = true
            Player.SettleParams.bIsFinished = true
            DOREPONCE(Player, "SettleParams")
        end
    else
        print("UGCSettlement:LuaExecuteWithFinish AllPlayer is nil")
    end

end

return UGCSettlement
```


### 示例 6

```lua
-- UGCGameMode.lua

local UGCGameMode = {}

function UGCGameMode:ReceiveBeginPlay()

	-- 启用关卡流程
	local MgrPath = "Asset/Blueprint/GameModeActor/SingleMode/SingleModeMgr.SingleModeMgr_C"
	UGCLevelFlowSystem.EnableLevelFlow(UGCGameSystem.GetUGCResourcesFullPath(MgrPath))
end

return UGCGameMode;
```


### 示例 7

```lua
-- MonsterSpawnerManager.lua

local MonsterSpawnerManager = {}

function MonsterSpawnerManager:OnAllMobDie()
    local PlayerControllers = UGCGameSystem.GetAllPlayerController(true)
        for k, v in pairs(PlayerControllers) do
            local TeamID = v.TeamID
            local Score = self.Score
            UGCLevelFlowSystem.LevelAddScore(TeamID, Score)
        end
end

return MonsterSpawnerManager
```


### 示例 8

```lua
-- PortalDoor.lua

---@field Trigger UCapsuleComponent
local PortalDoor = { };

function PortalDoor:ReceiveBeginPlay()

    PortalDoor.SuperClass.ReceiveBeginPlay(self)
    if UGCGameSystem.IsServer() then 
        self:InitDoor()
    end

end

function PortalDoor:InitDoor()
    self.Trigger.OnComponentBeginOverlap:Add(self.OnBeginOverlap, self)
end

function PortalDoor:ReceiveEndPlay()
    print(string.format("[PortalDoor:ReceiveEndPlay]"))
    self.Trigger.OnComponentBeginOverlap:Remove(self.OnBeginOverlap, self)
end

function PortalDoor:OnBeginOverlap(OverlappedComp, Actor, OtherComp, OtherBodyIndex, bFromSweep, SweepResult)

    -- 当玩家的Pawn和传送门折叠后，触发传送玩家
    UGCLevelFlowSystem.GoToNextLevelForOnePlayer(UGCGameSystem.GetPlayerControllerByPlayerKey(Actor.PlayerKey))
    
end

return PortalDoor
```


## 全局治疗公式

> 来源: 进阶内容/GamePlay系统/属性与伤害 (id:20216)


### 示例 1

```lua
local RecoverTypeTags = UGCAttributeSystem.GetDamageTagsFromContext(Context)
 
for _, Tag in pairs(RecoverTypeTags) do
	if Tag then
		print("[UGCGlobalRecoveryCalculation] Context Tag: --->"..Tag)
	end
end
```


### 示例 2

```lua
local CritTag = UGCGameplayTagSystem.RequestGameplayTag("UGC.Damage.Result.Critical")
if CritTag then
	ExtraResult.ResultTags:Add(CritTag)
end
```


## 1.33 Release Notes

> 来源: 版本日志 (id:20222)


### 示例 1

```lua
-- 如果函数有对应的Async类型函数，也包含在内，例如：UGCGameSystem.AsyncGetTableData
UGCGameSystem.GetUGCResourcesFullPath
UGCGameSystem.GetTableData
UGCGameSystem.GetTableCount
UGCGameSystem.GetDataTableData
UGCGameSystem.GetDataTableDataByRowName
UGCGameSystem.GetDataTableRowCount
LoadClass
LoadObject
UE.LoadClass
UE.LoadObject
```


## 结算模板

> 来源: 商业化及功能模板 (id:20232)


### 示例 1

```lua
-- 生效范围：服务器
---@param PlayerResults table @玩家结算数据
---@param TeamResults table @队伍结算数据
---@param bDraw bool @是否平局
function GameResultManager:BeginSettlement(PlayerResults, TeamResults, bDraw)
end
```


### 示例 2

```lua
-- 生效范围：服务器
function GameResultManager:EndSettlement()
end
```


### 示例 3

```lua
-- 生效范围：客户端
GameResultManager.OnSettlementFinished [MulticastDelegate] fun()
```


## 任务模板

> 来源: 商业化及功能模板 (id:20237)


### 示例 1

```lua
--UGCGameState.lua部分代码
function UGCGameState:ReceiveBeginPlay()
    self.SuperClass.ReceiveBeginPlay(self);
    if self:HasAuthority() == true then 
        -- 只有客户端加载UI
    else
        local TaskBtnClass = UGCObjectUtility.LoadClass(UGCGameSystem.GetUGCResourcesFullPath("ExtendResource/TaskTemplate/OfficialPackage/Asset/Task/Blueprint/WBP_TaskMainUIButton.WBP_TaskMainUIButton_C"));
        local TaskBtn = UGCWidgetManagerSystem.CreateWidget(TaskBtnClass);
        TaskBtn:AddToViewport();
    end
end
```


## 战斗日志

> 来源: 绿洲编辑器基础内容/调试游戏 (id:20242)


### 示例 1

```lua
ugcprint("click trigger", "SkillSlot")
```


## 动态更新导航网格

> 来源: 进阶内容/GamePlay系统/怪物系统 (id:20274)


### 示例 1

```lua
local min  = UGCMathUtility.MakeVector(18610.0,21590.0,0)
 local max  = UGCMathUtility.MakeVector(20660.0,22530.0,520.0)
 local Fbox = UGCMathUtility.MakeBox(min,max)
 UGCNavigationSystem.AddDynamicNavAffect(self, "Mannequin", Fbox)
```


### 示例 2

```lua
---生效范围：服务器
---@param WorldContext UObject 当前世界上下文
---@param AgentName FName 作用Agent的寻路图名称一般为"Mannequin"
UGCNavigationSystem.AsyncIncrementalBuild(self, "Mannequin")
```


### 示例 3

```lua
local FinishedDelegate = UGCNavigationSystem.GetNavigationGenerationFinishedDelegate(self)
FinishedDelegate:Add(self.OnFinishGeneration, self)
```


## 输入映射

> 来源: 进阶内容/输入系统 (id:20290)


### 示例 1

```lua
-- 在PlayerPawn中绑定输入事件并实现回调逻辑
function UGCPlayerPawn:ReceiveBeginPlay()
    UGCPlayerPawn.SuperClass.ReceiveBeginPlay(self)

    if not UGCGameSystem.IsServer() then
        local ActionTag = UGCGameplayTagSystem.RequestGameplayTag("Input.Action.Test")
        UGCInputSystem.BindInputMapping(self, ActionTag, ETriggerEvent.Triggered, self.OnTriggerJumpAction)
    end
end

function UGCPlayerPawn:OnTriggerJumpAction(InputValue, ElapsedTime, TriggeredTime, InputTag)
    local selfPC = UGCGameSystem.GetPlayerControllerByPlayerPawn(self)
    if selfPC then
        UnrealNetwork.CallUnrealRPC(selfPC, selfPC, "ServerRPC_TriggerSprintSkill");
    end
end

-- 服务端触发冲刺技能的释放
function UGCPlayerController:ServerRPC_TriggerSprintSkill()
		local PlayerPawn = UGCGameSystem.GetPlayerPawnByPlayerController(self)
    local TriggerSkill = UGCPersistEffectSystem.AddSkillByClass(PlayerPawn, UGCGameSystem.GetUGCResourcesFullPath('Asset/Blueprint/Prefabs/Skills/Sprint_test.Sprint_test_C'))
    TriggerSkill:ActivateSkill()
end
```


### 示例 2

```lua
local ActionTag = UGCGameplayTagSystem.RequestGameplayTag("Input.Action.Test")
UGCInputSystem.InjectInputMapping(self, ActionTag, 1)
```


## 载具刷新器

> 来源: 进阶内容/GamePlay系统/载具系统 (id:20291)


### 示例 1

```lua
local ToolClass = UGCObjectUtility.LoadClass('/Game/UGC/UGCGame/Vehicle/Refresher/BP_UGCVehicleRefresherTool.BP_UGCVehicleRefresherTool_C')
local ToolActors = UGCActorComponentUtility.GetAllActorsOfClass(self, ToolClass)
if #ToolActors > 0 then
	ToolActors[1]:GenerateVehicle()
end
```


## 通用屏幕指示器

> 来源: 进阶内容/UI系统 (id:20295)


### 示例 1

```lua
function WBP_UGC_MonsterHealthBar:Event_InitParam()
    ---@field SetStateWidgetPanel:fun(InScreenPanel:UWidget,OutScreenPanel:UWidget,InArrowWidget:UWidget,InDistanPanel:UWidget,InDistanText:UTextBlock)
    self:SetStateWidgetPanel(self.InScreenPnl, self.OutScreenPnl, self.InArrowWidget, nil, nil)
end
```


### 示例 2

```lua
-- @param InScreenPanel UWidget 对象在屏幕中显示的控件
-- @param OutScreenPanel UWidget 对象在屏幕外显示的控件
-- @param InArrowWidget UWidget 对象在屏幕外的时候需要显示的箭头控件，用来提示对象的方位，此控件会因对象的位置变化而产生一定的旋转
-- @param InDistanPanel UWidget 显示距离的控件
-- @param InDistanText UTextBlock 显示距离的文本控件，一般需要放在`InDistanPanel`里面
SetStateWidgetPanel:fun(InScreenPanel:UWidget,OutScreenPanel:UWidget,InArrowWidget:UWidget,InDistanPanel:UWidget,InDistanText:UTextBlock)
```


## 技能元件

> 来源: 进阶内容/UI系统 (id:20325)


### 示例 1

```lua
function TestSkillUI:Construct()
	TestSkillUI.SuperClass.InitCDProgress(self, self.Text_Time, self.Image_CDTime, self.CanvasPanel_CDtime)
end
```


### 示例 2

```lua
function TestSkillUI:OnSkillBound_BP(InOwnerSkill)
	TestSkillUI.SuperClass.OnSkillBound_BP(self, InOwnerSkill)

    if UE.IsValid(InOwnerSkill) then
        self.PreCDState = InOwnerSkill.SkillCD.MaxLayer ~= InOwnerSkill.SkillCD.CurLayer
        self.PreEnableState = InOwnerSkill:IsSkillEnable()
    end
end
```


## 补偿接入指引

> 来源: 商业化及功能模板 (id:20347)


### 示例 1

```lua
---补偿玩家商品后广播
---生效范围：服务器&客户端
---@param PlayerKey number @玩家PlayerKey
---@param UID number @玩家UID
---@param CommodityID number @物品ID
---@param Count number @物品数量
---@param ProductID number @商品ID
UGCCommoditySystem.CompensateUGCCommodityDelegate = Delegate.New()
```


### 示例 2

```lua
---补偿玩家商品后批量广播
---生效范围：服务器
---@param PlayerKey number @玩家PlayerKey
---@param UID number @玩家UID
---@param CommodityList table @补偿列表（含多个商品信息）
UGCCommoditySystem.CompensateUGCCommodityBatchDelegate = Delegate.New()
```


### 示例 3

```lua
CommodityList = {
    {
        CommodityID = 1001,  -- 物品ID（对应UGCObject表）
        Count       = 5,     -- 物品数量（正整数）
        ProductID   = 900001 -- 商品ID（对应UGCShop表）
    },
    {
        CommodityID = 1002,
        Count       = 3,
        ProductID   = 900002
    },
    -- ... 更多商品项
}
```


### 示例 4

```lua
-- 在 Server 或 Client 端注册监听（建议在玩家初始化时注册）
function MySystem:InitializeCompensateListener()
    -- 监听单笔补偿
    UGCCommoditySystem.CompensateUGCCommodityDelegate:Add(self, self.OnCompensateUGCCommodity)
end

---单笔补偿回调
---@param PlayerKey number
---@param UID number
---@param CommodityID number
---@param Count number
---@param ProductID number
function MySystem:OnCompensateUGCCommodity(PlayerKey, UID, CommodityID, Count, ProductID)
    print_dev(StringFormat_Dev("[MySystem] 收到单笔补偿 UID=%d CommodityID=%d Count=%d ProductID=%d", 
        UID, CommodityID, Count, ProductID))
    
    -- 示例：记录补偿日志
    self:LogCompensation(PlayerKey, UID, CommodityID, Count, ProductID)
    
    -- 示例：客户端弹提示（仅客户端需要）
    if not UGCGameSystem.IsServer() then
        self:ShowCompensationTip(CommodityID, Count)
    end
end
```


### 示例 5

```lua
-- 仅在服务器端注册
function MySystem:InitializeBatchCompensateListener()
    if not UGCGameSystem.IsServer() then
        return
    end
    
    -- 监听批量补偿
    UGCCommoditySystem.CompensateUGCCommodityBatchDelegate:Add(self.OnCompensateUGCCommodityBatch, self)
end

---批量补偿回调
---@param PlayerKey number
---@param UID number
---@param CommodityList table
function MySystem:OnCompensateUGCCommodityBatch(PlayerKey, UID, CommodityList)
    print_dev(StringFormat_Dev("[MySystem] 收到批量补偿 UID=%d 商品数量=%d", 
        UID, #CommodityList))
    
    -- 遍历补偿列表处理每一项
    for _, item in ipairs(CommodityList) do
        print_dev(StringFormat_Dev("[MySystem] 补偿项 CommodityID=%d Count=%d ProductID=%d", 
            item.CommodityID, item.Count, item.ProductID))
        
        -- 示例：写入数据库或运营日志
        self:RecordCompensationToDB(UID, item.CommodityID, item.Count, item.ProductID)
    end
    
    -- 示例：发送运营事件上报
    self:ReportCompensationEvent(UID, CommodityList)
end
```


### 示例 6

```lua
function MySystem:FinalizeCompensateListener()
    -- 移除单笔补偿监听
    UGCCommoditySystem.CompensateUGCCommodityDelegate:Remove(self.OnCompensateUGCCommodity, self)
    
    -- 移除批量补偿监听（如果在服务器注册了）
    if UGCGameSystem.IsServer() then
        UGCCommoditySystem.CompensateUGCCommodityBatchDelegate:Remove(self.OnCompensateUGCCommodityBatch, self)
    end
end
```


## Tween功能

> 来源: 通用功能 (id:20351)


### 示例 1

```lua
Linear,
QuadIn, QuadOut, QuadInOut,
CubicIn, CubicOut, CubicInOut,
QuartIn, QuartOut, QuartInOut,
QuintIn, QuintOut, QuintInOut,
SineIn, SineOut, SineInOut,
ExpoIn, ExpoOut, ExpoInOut,
CircIn, CircOut, CircInOut,
ElasticIn, ElasticOut, ElasticInOut,
BackIn, BackOut, BackInOut,
BounceIn, BounceOut, BounceInOut
```


### 示例 2

```lua
local Config = UGCTweenSystem.MakeConfig(0, 1, false, 0)
```


### 示例 3

```lua
local TweenSubsystem = UGCTweenSystem.GetTweenSubsystem()
```


### 示例 4

```lua
local Callback = function(Object, Value)
    -- Object 是系统自动传入的占位符（第一个参数）
    -- Value 是当前动画的数值（第二个参数）
    -- 在这里将 Value 应用到目标对象
    self.TargetWidget:SetColorAndOpacity(Value)
end
```


### 示例 5

```lua
function TweenColor:Construct()
    -- 定义起始颜色和目标颜色
    local StartColor = KismetMathLibrary.MakeColor(0, 1, 0, 1)  -- 绿色
    local EndColor = KismetMathLibrary.MakeColor(1, 0, 0, 1)    -- 红色

    -- 创建回调函数
    local Callback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        self.TweenTarget:SetColorAndOpacity(Value)
    end

    -- 配置动画参数
    local Config = UGCTweenSystem.MakeConfig(0, -1, false, 0)

    -- 创建颜色动画（1秒持续时间，使用 Linear 缓动）
    self.TweenHandler = UGCTweenSystem.TweenColorValue(
        StartColor,
        EndColor,
        1.0,
        self.TweenType,        -- 缓动类型
        Callback,
        Config
    )

    -- 暂停动画
    UGCTweenSystem.PauseTween(self.TweenHandler)
end
```


### 示例 6

```lua
function TweenMove:Construct()
    -- 定义起始位置和目标位置
    local StartLocation = UGCMathUtility.MakeVector(0, 0, 0)
    local EndLocation = UGCMathUtility.MakeVector(700, 0, 0)

    -- 创建回调函数
    local Callback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        local Slot = UGCWidgetManagerSystem.SlotAsCanvasSlot(self.TweenImage)
        Slot:SetPosition(UGCMathUtility.MakeVector2D(Value.X, 0))
    end

    -- 配置动画参数
    local Config = UGCTweenSystem.MakeConfig(0, -1, false, 0)

    -- 创建向量动画（1秒持续时间）
    self.TweenHandler = UGCTweenSystem.TweenVectorValue(
        StartLocation,
        EndLocation,
        1.0,
        self.TweenType,
        Callback,
        Config
    )

    -- 暂停动画
    UGCTweenSystem.PauseTween(self.TweenHandler)
end
```


### 示例 7

```lua
function MoveActor:Construct()
    local Actor = UGCActorComponentUtility.GetActorByActorInstancePath("UGCmap.MyActor")

    -- 定义目标位置
    local TargetLocation = UGCMathUtility.MakeVector(100, 100, 100)

    -- 配置动画参数
    local Config = UGCTweenSystem.MakeConfig(0, 0, false, 0)

    -- 创建 Actor 位置动画
    local TweenHandler = UGCTweenSystem.TweenActorLocation(
        Actor,
        TargetLocation,
        2.0,
        EEasingType.EaseOutQuad,
        Config
    )
end
```


### 示例 8

```lua
function PauseCheckBox_OnCheckStateChanged(bIsChecked)
    if not self.TweenHandler then
        return
    end

    if bIsChecked then
        -- 暂停动画
        UGCTweenSystem.PauseTween(self.TweenHandler)
    else
        -- 恢复动画
        UGCTweenSystem.ResumeTween(self.TweenHandler)
    end
end
```


### 示例 9

```lua
function YoyoCheckBox_OnCheckStateChanged(bIsChecked)
    if not self.TweenHandler then
        return
    end

    -- 动态修改动画配置
    UGCTweenSystem.ConfigureTween(
        self.TweenHandler,
        0,               -- Delay
        -1,              -- RepeatCount（-1 表示无限循环）
        bIsChecked,      -- bYoyo
        0                -- RepeatDelay
    )
end
```


### 示例 10

```lua
function StopAnimation()
    -- 停止动画，对象会回到起始值
    UGCTweenSystem.KillTween(TweenHandler)
end
```


### 示例 11

```lua
function ChainAnimations()
    -- 创建第一个动画
    local Handler1 = UGCTweenSystem.TweenColorValue(
        StartColor1,
        EndColor1,
        1.0,
        EEasingType.Linear,
        Callback1,
        Config1
    )

    -- 创建第二个动画
    local Handler2 = UGCTweenSystem.TweenColorValue(
        StartColor2,
        EndColor2,
        1.0,
        EEasingType.EaseInQuad,
        Callback2,
        Config2
    )

    -- 链接动画：Handler1 完成后自动执行 Handler2
    UGCTweenSystem.ChainTween(Handler1, Handler2)
end
```


### 示例 12

```lua
function BindCompletionCallback()
    -- 绑定到 Tween
    UGCTweenSystem.BindCompletedDelegate(TweenHandler, function()
        print("动画完成！")
        -- 执行后续逻辑
    end)
end
```


### 示例 13

```lua
function CheckTweenState()
    local IsValid = UGCTweenSystem.IsTweenValid(TweenHandler)
    if IsValid then
        print("动画正在执行")
    else
        print("动画已停止")
    end
end
```


### 示例 14

```lua
function Button_OnClicked()
    local Button = self.ClickButton

    -- 定义缩放比例
    local StartScale = 1.0
    local EndScale = 1.2

    -- 创建回调函数
    local Callback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        Button:SetRenderScale(UGCMathUtility.MakeVector2D(Value, Value))
    end

    -- 配置动画
    local Config = UGCTweenSystem.MakeConfig(0, 0, true, 0)

    -- 创建浮点动画（0.2秒）
    local TweenHandler = UGCTweenSystem.TweenFloatValue(
        StartScale,
        EndScale,
        0.2,
        EEasingType.EaseOutQuad,
        Callback,
        Config
    )
end
```


### 示例 15

```lua
function FadeInUI()
    local UIWidget = self.FadeWidget

    -- 定义透明度
    local StartOpacity = 0.0
    local EndOpacity = 1.0

    -- 创建回调函数
    local Callback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        UIWidget:SetRenderOpacity(Value)
    end

    -- 配置动画
    local Config = UGCTweenSystem.MakeConfig(0, 0, false, 0)

    -- 创建浮点动画（1.5秒）
    local TweenHandler = UGCTweenSystem.TweenFloatValue(
        StartOpacity,
        EndOpacity,
        1.5,
        EEasingType.EaseInQuad,
        Callback,
        Config
    )
end
```


### 示例 16

```lua
function LoadingProgress()
    local ProgressText = self.ProgressText

    -- 定义进度
    local StartProgress = 0.0
    local EndProgress = 100.0

    -- 创建回调函数
    local Callback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        -- 更新进度文本
        ProgressText:SetText(string.format("加载中: %.1f%%", Value))
    end

    -- 配置动画
    local Config = UGCTweenSystem.MakeConfig(0.5, 0, false, 0)

    -- 创建浮点动画（5秒）
    local TweenHandler = UGCTweenSystem.TweenFloatValue(
        StartProgress,
        EndProgress,
        5.0,
        EEasingType.Linear,
        Callback,
        Config
    )

    -- 绑定完成回调
    UGCTweenSystem.BindCompletedDelegate(TweenHandler, function()
        print("加载完成！")
        ProgressText:SetText("加载完成: 100%")
    end)
end
```


### 示例 17

```lua
function MoveObject()
    local Object = UGCActorComponentUtility.GetActorByActorInstancePath("UGCmap.TargetObject")

    -- 定义起始位置和目标位置
    local StartLocation = Object:GetActorLocation()
    local TargetLocation = UGCMathUtility.MakeVector(500, 500, 200)

    -- 配置动画
    local Config = UGCTweenSystem.MakeConfig(0, 0, false, 0)

    -- 创建 Actor 位置动画（2秒）
    local TweenHandler = UGCTweenSystem.TweenActorLocation(
        Object,
        TargetLocation,
        2.0,
        EEasingType.EaseInOutQuad,
        Config
    )

    -- 绑定完成回调
    UGCTweenSystem.BindCompletedDelegate(TweenHandler, function()
        print("物体已到达目标位置")
    end)
end
```


### 示例 18

```lua
function CombinedAnimationSequence()
    local UIWidget = self.AnimatedWidget

    -- 步骤1：淡入
    local FadeCallback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        UIWidget:SetRenderOpacity(Value)
    end

    local FadeConfig = UGCTweenSystem.MakeConfig(0, 0, false, 0)

    local FadeHandler = UGCTweenSystem.TweenFloatValue(
        0.0,
        1.0,
        1.0,
        EEasingType.EaseInQuad,
        FadeCallback,
        FadeConfig
    )

    -- 步骤2：移动
    local MoveCallback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        local Slot = UGCWidgetManagerSystem.SlotAsCanvasSlot(UIWidget)
        Slot:SetPosition(UGCMathUtility.MakeVector2D(Value.X, Value.Y))
    end

    local MoveConfig = UGCTweenSystem.MakeConfig(0, 0, false, 0)

    local MoveHandler = UGCTweenSystem.TweenVectorValue(
        UGCMathUtility.MakeVector(0, 0, 0),
        UGCMathUtility.MakeVector(200, 100, 0),
        1.0,
        EEasingType.EaseOutQuad,
        MoveCallback,
        MoveConfig
    )

    -- 步骤3：缩放
    local ScaleCallback = function(Object, Value)
        -- Object 是系统自动传入的占位符
        UIWidget:SetRenderScale(UGCMathUtility.MakeVector2D(Value, Value))
    end

    local ScaleConfig = UGCTweenSystem.MakeConfig(0, 0, false, 0)

    local ScaleHandler = UGCTweenSystem.TweenFloatValue(
        1.0,
        1.5,
        0.5,
        EEasingType.EaseOutQuad,
        ScaleCallback,
        ScaleConfig
    )

    -- 链接动画序列
    UGCTweenSystem.ChainTween(FadeHandler, MoveHandler)
    UGCTweenSystem.ChainTween(MoveHandler, ScaleHandler)
end
```


### 示例 19

```lua
local Config = UGCTweenSystem.MakeConfig(0, 1, false, 0)
```


### 示例 20

```lua
UGCTweenSystem.ChainTween(Handler1, Handler2)
```


### 示例 21

```lua
UGCTweenSystem.PauseTween(TweenHandler)
```


### 示例 22

```lua
UGCTweenSystem.ResumeTween(TweenHandler)
```


### 示例 23

```lua
UGCTweenSystem.KillTween(TweenHandler)
```


### 示例 24

```lua
UGCTweenSystem.BindCompletedDelegate(TweenHandler, function()
    print("动画完成！")
end)
```


### 示例 25

```lua
local Config = UGCTweenSystem.MakeConfig(2.0, 0, false, 0)  -- 延迟 2 秒后执行
```


### 示例 26

```lua
-- 无限循环
local Config = UGCTweenSystem.MakeConfig(0, -1, false, 0)

-- 播放 3 次（1 次初始 + 2 次重复）
local Config = UGCTweenSystem.MakeConfig(0, 2, false, 0)
```


### 示例 27

```lua
local Config = UGCTweenSystem.MakeConfig(0, -1, true, 0)  -- 往返循环
```


### 示例 28

```lua
-- 创建三个动画
local Handler1 = UGCTweenSystem.TweenFloatValue(...)
local Handler2 = UGCTweenSystem.TweenFloatValue(...)
local Handler3 = UGCTweenSystem.TweenFloatValue(...)

-- 链接动画序列
UGCTweenSystem.ChainTween(Handler1, Handler2)
UGCTweenSystem.ChainTween(Handler2, Handler3)
```


### 示例 29

```lua
UGCTweenSystem.ConfigureTween(
    TweenHandler,
    0,          -- Delay
    -1,         -- RepeatCount
    true,       -- bYoyo
    0           -- RepeatDelay
)
```


## Actor实例路径使用

> 来源: 绿洲编辑器基础内容/脚本编写 (id:20352)


### 示例 1

```lua
-- 通过实例路径获取 Actor
local Actor = UGCActorComponentUtility.GetActorByActorInstancePath("UGCmap.test_8")

-- 验证 Actor 有效性并使用
if Actor and UGCObjectUtility.IsObjectValid(Actor) then
    ugcprint_concat("Find Actor Success", Actor:GetName())
    -- 后续可以对该 Actor 进行操作
end
```


### 示例 2

```lua
-- 在 Lua 配置中直接引用场景中的目标对象
local TargetActorPath = "UGCmap.Dynamic_ShootingRange_Target5"
local TargetActor = UGCActorComponentUtility.GetActorByActorInstancePath(TargetActorPath)

if TargetActor then
    -- 对目标对象进行配置或操作
    TargetActor:SetActorLocation(Vector.New(100, 100, 100))
end
```


### 示例 3

```lua
-- 关卡脚本通过路径查找预设的点位
local CheckpointPath = "UGCmap.CheckPoint_1"
local CheckpointActor = UGCActorComponentUtility.GetActorByActorInstancePath(CheckpointPath)

if CheckpointActor then
    -- 检查玩家是否到达检查点
    local Player = UGCPlayerController.GetPawn()
    if Player then
        local Distance = Player:GetDistanceTo(CheckpointActor)
        if Distance < 10.0 then
            -- 到达检查点，触发相关逻辑
        end
    end
end
```


### 示例 4

```lua
-- 通过路径查找场景中的触发器或机关
local TriggerPath = "UGCmap.Trigger_Gate1"
local TriggerActor = UGCActorComponentUtility.GetActorByActorInstancePath(TriggerPath)

if TriggerActor then
    -- 监听触发器事件或进行激活操作
    TriggerActor:Activate()
end
```


### 示例 5

```lua
-- 批量复制多个实例路径后，在脚本中统一处理
local TargetPaths = {
    "UGCmap.Target1",
    "UGCmap.Target2",
    "UGCmap.Target3"
}

for i, Path in ipairs(TargetPaths) do
    local TargetActor = UGCActorComponentUtility.GetActorByActorInstancePath(Path)
    if TargetActor then
        -- 对每个目标进行统一操作
        TargetActor:SetActorScale3D(Vector.New(2, 2, 2))
    end
end
```


## UGC 商业化接口库

> 来源: 商业化及功能模板 (id:20377)


### 示例 1

```lua
---生效范围：服务器&客户端
---@param PlayerKey number @玩家PlayerKey，服务器调用必传
---@return number @玩家货币数量
function UGCCommoditySystem.GetTicket(PlayerKey)
```


### 示例 2

```lua
-- 客户端调用
local ticket = UGCCommoditySystem.GetTicket()

-- 服务器调用
local ticket = UGCCommoditySystem.GetTicket(playerController.PlayerKey)
```


### 示例 3

```lua
---生效范围：客户端
---@param BuyCommodityCMD string @购买协议
---@param Name string @物品名称
---@param Icon string @物品图标
---@param Desc string @物品描述
---@param Count number @单次购买数量
---@param Cost number @单个物品价格，必须传入 GetSellingPriceAfterDiscount 的返回值
---@return PromiseFuture @二次确认弹窗界面对象实例的 PromiseFuture 对象
function UGCCommoditySystem.BuyUGCCommodity(BuyCommodityCMD, Name, Icon, Desc, Count, Cost)
```


### 示例 4

```lua
---生效范围：客户端
---@param ProductID number @商品ID
---@param Icon string @物品图标
---@param Desc string @物品描述
---@param Count number @单次购买数量
---@return PromiseFuture @二次确认弹窗界面对象实例的 PromiseFuture 对象
function UGCCommoditySystem.BuyUGCCommodity2(ProductID, Icon, Desc, Count)
```


### 示例 5

```lua
---生效范围：客户端
---@param BuyCommodityCMD string @购买协议
---@param Name string @物品名称
---@param Icon string @物品图标
---@param Desc string @物品描述
---@param Count number @单次购买数量
---@param Cost number @单个物品价格
---@return PromiseFuture
function UGCCommoditySystem.BuyUGCPrivilegeCommodity(BuyCommodityCMD, Name, Icon, Desc, Count, Cost)
```


### 示例 6

```lua
---生效范围：客户端
---@param UseCommodityCMD string @使用协议
---@param CommodityID number @物品ID
---@param Name string @物品名称
---@param Icon string @物品图标
---@param Desc string @物品描述
---@param Count number @单次消耗数量
---@param bShowDialog boolean @是否显示二次确认
---@return PromiseFuture @bShowDialog 为 true 时返回二次确认弹窗的 PromiseFuture
function UGCCommoditySystem.UseUGCCommodity(UseCommodityCMD, CommodityID, Name, Icon, Desc, Count, bShowDialog)
```


### 示例 7

```lua
---生效范围：服务器&客户端
---@param PlayerController PlayerController @玩家控制器，服务器端必传
---@param ObjectID number @虚拟物品ID
---@param Icon string @虚拟物品图标，填 nil 则使用 UGCObject.ItemSmallIcon
---@param Desc string @虚拟物品描述，填 nil 则使用 UGCObject.ItemDesc
---@param Count number @单次消耗数量
---@param bShowDialog boolean @是否显示二次确认（服务器端忽略）
---@return PromiseFuture
function UGCCommoditySystem.UseUGCCommodity2(PlayerController, ObjectID, Icon, Desc, Count, bShowDialog)
```


### 示例 8

```lua
---生效范围：服务器
---@param BuyCommodityCMD string @购买协议
---@param ProductID number @商品ID
---@param Count number @单次购买数量
function UGCCommoditySystem.RegBuyCMD(BuyCommodityCMD, ProductID, Count)
```


### 示例 9

```lua
---生效范围：服务器
---@param UseCommodityCMD string @使用协议
---@param CommodityID number @物品ID
---@param Count number @单次消耗数量
function UGCCommoditySystem.RegUseCMD(UseCommodityCMD, CommodityID, Count)
```


### 示例 10

```lua
---生效范围：服务器 & 客户端
---@param PlayerKey number @玩家PlayerKey，服务器调用必传
---@return table @玩家的物品数据列表 { {CommodityID=xxx, Count=xxx}, ... }
function UGCCommoditySystem.GetUGCCommodityList(PlayerKey)
```


### 示例 11

```lua
---生效范围：服务器
---@return table @所有玩家的物品数据 { UID: [{CommodityID, Count}, ...] }
function UGCCommoditySystem.GetAllPlayerUGCCommodityList()
```


### 示例 12

```lua
---生效范围：客户端
---@return table @玩家的商品限购数据列表 { {ProductID=xxx, BuyProductLimitCount=xxx}, ... }
function UGCCommoditySystem.GetUGCProductList()
```


### 示例 13

```lua
---生效范围：服务器
---@return table @所有玩家的商品限购数据
function UGCCommoditySystem.GetAllPlayerUGCProductList()
```


### 示例 14

```lua
---生效范围：服务器 & 客户端
---@param BuyCommodityCMDOrProductID string|number @购买协议或商品ID
---@return number @折扣后价格，参数无效返回 nil
function UGCCommoditySystem.GetSellingPriceAfterDiscount(BuyCommodityCMDOrProductID)
```


### 示例 15

```lua
-- 方式1: 通过协议
local price = UGCCommoditySystem.GetSellingPriceAfterDiscount("BuyWeapon")

-- 方式2: 通过ProductID
local price = UGCCommoditySystem.GetSellingPriceAfterDiscount(900001)
```


### 示例 16

```lua
---生效范围：客户端
---@return PromiseFuture @充值界面对象的 PromiseFuture
function UGCCommoditySystem.ShowRechargeEntryUI()
```


### 示例 17

```lua
生效范围：客户端
function UGCCommoditySystem.ClearCommodity()
```


### 示例 18

```lua
---@param bSuccess boolean @是否成功
---@param PlayerKey number @玩家PlayerKey
---@param CommodityID number @物品ID
---@param Count number @数量
---@param UID number @玩家UID
---@param ProductID number @商品ID
UGCCommoditySystem.BuyUGCCommodityResultDelegate
```


### 示例 19

```lua
UGCCommoditySystem.BuyUGCCommodityResultDelegate:Add(function(bSuccess, PlayerKey, CommodityID, Count, UID, ProductID)
    if bSuccess then
        print("购买成功:", CommodityID, Count)
    else
        print("购买失败")
    end
end)
```


### 示例 20

```lua
---@param bSuccess boolean @是否成功
---@param PlayerKey number @玩家PlayerKey
---@param CommodityID number @物品ID
---@param Count number @数量
---@param UID number @玩家UID
---@param ProductID number @商品ID
UGCCommoditySystem.UseUGCCommodityResultDelegate
```


### 示例 21

```lua
---@param PlayerKey number @玩家PlayerKey
---@param UID number @玩家UID
---@param CommodityID number @物品ID
---@param Count number @数量
---@param ProductID number @商品ID
UGCCommoditySystem.CompensateUGCCommodityDelegate
```


### 示例 22

```lua
---@param PlayerKey number @玩家PlayerKey
---@param UID number @玩家UID
UGCCommoditySystem.UGCCommodityPlayerDataChangedDelegate
```


### 示例 23

```lua
---@param PlayerKey number @玩家PlayerKey
---@param UID number @玩家UID
UGCCommoditySystem.UGCProductsChangedDelegate
```


### 示例 24

```lua
---@param PlayerKey number @玩家PlayerKey
---@param UID number @玩家UID
---@param CommodityID number @物品ID
---@param Count number @数量
UGCCommoditySystem.BuyUGCCommodityResultBetweenGamesDelegate
```


### 示例 25

```lua
-- 1. 服务器注册购买协议（GameMode初始化时）
UGCCommoditySystem.RegBuyCMD("BuyWeapon", 900001, 1)

-- 2. 客户端获取商品价格
local price = UGCCommoditySystem.GetSellingPriceAfterDiscount(900001)

-- 3. 显示商品信息并购买
UGCCommoditySystem.BuyUGCCommodity2(
    900001,              -- ProductID
    nil,                 -- Icon（自动使用配置）
    nil,                 -- Desc（自动使用配置）
    1                    -- Count
)

-- 4. 监听购买结果
UGCCommoditySystem.BuyUGCCommodityResultDelegate:Add(function(bSuccess, PlayerKey, CommodityID, Count, UID, ProductID)
    print(string.format("购买结果: %s, 物品ID: %s, 数量: %d", bSuccess, CommodityID, Count))
end)
```


### 示例 26

```lua
-- 1. 服务器注册使用协议（GameMode初始化时）
UGCCommoditySystem.RegUseCMD("UsePotion", 1001, 1)

-- 2. 客户端使用物品（带确认）
UGCCommoditySystem.UseUGCCommodity2(
    nil,    -- PlayerController（客户端可传nil）
    1001,   -- ObjectID
    nil,    -- Icon（自动使用配置）
    nil,    -- Desc（自动使用配置）
    1,      -- Count
    true    -- bShowDialog
)

-- 3. 监听使用结果
UGCCommoditySystem.UseUGCCommodityResultDelegate:Add(function(bSuccess, PlayerKey, CommodityID, Count, UID)
    print(string.format("使用结果: %s, 物品ID: %s, 数量: %d", bSuccess, CommodityID, Count))
end)
```


### 示例 27

```lua
-- 查询当前玩家物品列表
local commodityList = UGCCommoditySystem.GetUGCCommodityList()
if commodityList then
    for _, commodity in ipairs(commodityList) do
        print(string.format("物品ID: %s, 数量: %d", commodity.CommodityID, commodity.Count))
    end
end

-- 查询当前玩家商品限购数据
local productList = UGCCommoditySystem.GetUGCProductList()
if productList then
    for _, product in ipairs(productList) do
        print(string.format("商品ID: %s, 已购买: %d", product.ProductID, product.BuyProductLimitCount))
    end
end
```


## 怪物

> 来源: 玩法案例及模板/俯视角生存模版 (id:20381)


### 示例 1

```lua
function BP_SpawnerManager:ApplyMonsterAttributes(Pawn)
    local MonsterConfig = TopDownSurvivor:GetMonsterConfig(Pawn.CharacterName)

    if MonsterConfig == nil then
        return
    end

    for AttributeType, Value in pairs(MonsterConfig.Attributes) do
        local GrowthType = GetGrowthTypeFromAttributeType(AttributeType)
        local GrowthValue = 1

        local CustomGrowth = MonsterConfig.CustomWaveGrowth[self.CurrentWaveIndex]
        if CustomGrowth and CustomGrowth[GrowthType] then
            GrowthValue = CustomGrowth[GrowthType]
        else
            local GrowthPerWave = MonsterConfig.GrowthPerWave[GrowthType] or 0
            GrowthValue = self.CurrentWaveIndex * GrowthPerWave
        end

        -- GameLog.Log("BP_SpawnerManager:ApplyMonsterAttributes", string.format("AttributeType: %s, GrowthValue: %f", AttributeType, GrowthValue))
        UGCAttributeSystem.SetGameAttributeValue(Pawn, AttributeType, Value + GrowthValue)
    end
end
```


## 技能

> 来源: 玩法案例及模板/俯视角生存模版 (id:20382)


### 示例 1

```lua
-- 将玩家角色始终朝向选择的目标
function Passive_AutoAim:SetControllerFaceTo()
    local Targets = self:GetSelectTargetActor(EPESkillSelectTarget.E_PESKILL_PickerType_AllTarget)
    -- GameLog.Log("AutoAim", string.format("Targets num: %d", #Targets))

    local Owner = self:GetOwnerActor()
    if Owner == nil or UGCPlayerPawnSystem.HasPawnState(Owner, EPawnState.Dead) then
        return
    end

    local PlayerController = UGCGameSystem.GetPlayerControllerByPlayerPawn(Owner)
    if PlayerController == nil then
        return
    end

    if Targets == nil or #Targets == 0 then
        return
    end

    for _, Target in ipairs(Targets) do
        local bTargetDead = UGCPersistEffectSystem.HasDynamicState(Target, "PawnState.Dead")

        if not bTargetDead then
            -- GameLog.Log("AutoAim", string.format("Face to target: %s", UGCObjectUtility.GetObjectName(Target)))

            local TargetLocation = Target:K2_GetActorLocation() 
            local OwnerLocation = Owner:K2_GetActorLocation()
            local Direction = UGCMathUtility.SubtractVector(TargetLocation, OwnerLocation)
            Direction.Z = 0
            Direction = UGCMathUtility.Normal(Direction)
            local Rotation = UGCMathUtility.Conv_VectorToRotator(Direction)
            UGCPlayerControllerSystem.SetControlRotation(PlayerController, Rotation)
            return
        end
    end
end
```


### 示例 2

```lua
function Melee:OnEnableSkill_BP()
    Melee.SuperClass.OnEnableSkill_BP(self)

    local OwnerActor = self:GetOwnerActor()

    self.AttackRange = UGCAttributeSystem.GetGameAttributeValue(OwnerActor, E_CharacterAttributeType.AttackRange)
    
    self.AttackRangeChangeDelegate = UGCAttributeSystem.AddGameAttributeChangedDelegate(OwnerActor, E_CharacterAttributeType.AttackRange,
        function (Owner, AttrName, NewValue)
            self.AttackRange = UGCAttributeSystem.GetGameAttributeValue(OwnerActor, E_CharacterAttributeType.AttackRange)
        end
    )
end

function Melee:OnDisableSkill_BP()
    Melee.SuperClass.OnDisableSkill_BP(self)

    local OwnerActor = self:GetOwnerActor()

    UGCAttributeSystem.RemoveGameAttributeChangedDelegate(OwnerActor, E_CharacterAttributeType.AttackRange, self.AttackRangeChangeDelegate)
end
```
